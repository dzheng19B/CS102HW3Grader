def lengthOfLongestSubstring(s: str) -> int:
	l = 0
	count = 0
	seen = set()
	for r in range(len(s)):
		if s[r] in seen:
			seen.remove(s[l])
			left += 1
		seen.add(s[r])
		count = max(count, s[r] - s[l] + 1)
	return count