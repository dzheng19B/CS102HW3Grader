# Python Grader Report

Generated: 2026-05-16 01:36  
Source CSV: `Homework 4 - Attempt Details_wr_filtered.csv`

> **How to read this report**  
> Each student section shows their _original_ submission exactly as it came
> from the CSV, followed by the _modified_ version the grader actually ran.
> The **Edits** list explains every change made and why.

---

## Summary Table

| Student                     | Question |     Score     | Status        |
| --------------------------- | -------- | :-----------: | ------------- |
| Abdoul Razakou Mahaman Sani | FBV      |     0/10      | TESTED        |
| Abdoul Razakou Mahaman Sani | TwoSum   |     9/10      | TESTED        |
| Albert Chen                 | FBV      |     6/10      | TESTED        |
| Albert Chen                 | TwoSum   |     8/10      | TESTED        |
| Alison Batz                 | FBV      |     0/10      | TESTED        |
| Alison Batz                 | TwoSum   |     0/10      | TESTED        |
| Andrew McBean               | FBV      |     10/10     | TESTED        |
| Andrew McBean               | TwoSum   |     10/10     | TESTED        |
| Ashley Carozza              | FBV      |     0/10      | TESTED        |
| Ashley Carozza              | TwoSum   |     10/10     | TESTED        |
| Ava Attina                  | FBV      |     1/10      | TESTED        |
| Ava Attina                  | TwoSum   |     0/10      | TESTED        |
| Brian Chen                  | FBV      |     10/10     | TESTED        |
| Brian Chen                  | TwoSum   |     0/10      | TESTED        |
| Cheong Ting Eland Chan      | FBV      |     8/10      | TESTED        |
| Cheong Ting Eland Chan      | TwoSum   |     0/10      | TESTED        |
| Christian Zuniga            | FBV      | NO SUBMISSION | NO SUBMISSION |
| Christian Zuniga            | TwoSum   | NO SUBMISSION | NO SUBMISSION |
| Chuyao Yu                   | FBV      |     10/10     | TESTED        |
| Chuyao Yu                   | TwoSum   |     10/10     | TESTED        |
| Clare Calandra              | FBV      |     10/10     | TESTED        |
| Danila Safronov             | FBV      |     10/10     | TESTED        |
| Danila Safronov             | TwoSum   |     10/10     | TESTED        |
| Dominic Vega                | FBV      |     0/10      | TESTED        |
| Dominic Vega                | TwoSum   |     8/10      | TESTED        |
| Elaine Zou                  | FBV      |     0/10      | TESTED        |
| Elaine Zou                  | TwoSum   |     9/10      | TESTED        |
| Ethan Wong                  | FBV      |  PSEUDOCODE   | PSEUDOCODE    |
| Ethan Wong                  | TwoSum   |  PSEUDOCODE   | PSEUDOCODE    |
| Evan Weisberg               | FBV      |     10/10     | TESTED        |
| Evan Weisberg               | TwoSum   |     10/10     | TESTED        |
| Gabriel Yang                | FBV      |     0/10      | TESTED        |
| Gabriel Yang                | TwoSum   |     10/10     | TESTED        |
| Gaven Chan                  | FBV      |     10/10     | TESTED        |
| Gaven Chan                  | TwoSum   |     9/10      | TESTED        |
| Gavin Ng                    | FBV      |     0/10      | TESTED        |
| Gavin Ng                    | TwoSum   |     9/10      | TESTED        |
| Hewitt Wang                 | FBV      |     0/10      | TESTED        |
| Hewitt Wang                 | TwoSum   |  PSEUDOCODE   | PSEUDOCODE    |
| Ian Porto                   | FBV      |     10/10     | TESTED        |
| Ian Porto                   | TwoSum   |     10/10     | TESTED        |
| Isabella Yang               | FBV      |     10/10     | TESTED        |
| Isabella Yang               | TwoSum   |     10/10     | TESTED        |
| Ivan Cheung                 | FBV      |     0/10      | TESTED        |
| Ivan Cheung                 | TwoSum   |     0/10      | TESTED        |
| Jack Stevens                | FBV      |     10/10     | TESTED        |
| Jack Stevens                | TwoSum   |     10/10     | TESTED        |
| Jake Steck                  | FBV      |     10/10     | TESTED        |
| Jake Steck                  | TwoSum   |     10/10     | TESTED        |
| Jason Guo                   | FBV      |  PSEUDOCODE   | PSEUDOCODE    |
| Jason Guo                   | TwoSum   |  PSEUDOCODE   | PSEUDOCODE    |
| Jason Seng                  | FBV      |     0/10      | TESTED        |
| Jason Seng                  | TwoSum   |     0/10      | TESTED        |
| Jin Noh                     | FBV      |     10/10     | TESTED        |
| Jin Noh                     | TwoSum   |     10/10     | TESTED        |
| Jordanny Ramos Rodriguez    | FBV      |  PSEUDOCODE   | PSEUDOCODE    |
| Jordanny Ramos Rodriguez    | TwoSum   |  PSEUDOCODE   | PSEUDOCODE    |
| Juan Moran                  | FBV      |     0/10      | TESTED        |
| Juan Moran                  | TwoSum   |     9/10      | TESTED        |
| Justin Yu                   | FBV      |     0/10      | TESTED        |
| Justin Yu                   | TwoSum   |     0/10      | TESTED        |
| Kartik Wahlin               | FBV      |     10/10     | TESTED        |
| Kartik Wahlin               | TwoSum   |     10/10     | TESTED        |
| Kathryn Schauber            | FBV      |     10/10     | TESTED        |
| Kathryn Schauber            | TwoSum   |     10/10     | TESTED        |
| Kenneth Ng                  | FBV      |     10/10     | TESTED        |
| Kenneth Ng                  | TwoSum   |     10/10     | TESTED        |
| Kushagra Singh              | FBV      |     10/10     | TESTED        |
| Kushagra Singh              | TwoSum   |     10/10     | TESTED        |
| Kyle Godzki                 | FBV      |     0/10      | TESTED        |
| Kyle Godzki                 | TwoSum   |     0/10      | TESTED        |
| Lilia Diusheyeva            | FBV      |     0/10      | TESTED        |
| Lilia Diusheyeva            | TwoSum   |     0/10      | TESTED        |
| Lilian Yuan                 | FBV      |     1/10      | TESTED        |
| Lilian Yuan                 | TwoSum   |     10/10     | TESTED        |
| Logan Regueiferos           | FBV      |     10/10     | TESTED        |
| Logan Regueiferos           | TwoSum   |     10/10     | TESTED        |
| Maddie Broderick            | FBV      |     10/10     | TESTED        |
| Maddie Broderick            | TwoSum   |     9/10      | TESTED        |
| Mahir Majlis                | FBV      |     0/10      | TESTED        |
| Mahir Majlis                | TwoSum   |     10/10     | TESTED        |
| Matthew Park                | FBV      |     10/10     | TESTED        |
| Matthew Park                | TwoSum   |     10/10     | TESTED        |
| Max Stehura                 | FBV      |     0/10      | TESTED        |
| Max Stehura                 | TwoSum   |     10/10     | TESTED        |
| Michael DiNapoli            | FBV      |     0/10      | TESTED        |
| Michael DiNapoli            | TwoSum   |     0/10      | TESTED        |
| Monica Gnajewski            | FBV      |     10/10     | TESTED        |
| Monica Gnajewski            | TwoSum   |     10/10     | TESTED        |
| Naman Kukreti               | FBV      |     0/10      | TESTED        |
| Naman Kukreti               | TwoSum   |     9/10      | TESTED        |
| Naomi Sellam                | FBV      |     10/10     | TESTED        |
| Naomi Sellam                | TwoSum   |     10/10     | TESTED        |
| Nicholas Friedlander        | FBV      |     0/10      | TESTED        |
| Nicholas Friedlander        | TwoSum   |     7/10      | TESTED        |
| Noel Maldonado              | FBV      |     10/10     | TESTED        |
| Noel Maldonado              | TwoSum   |     10/10     | TESTED        |
| Parks Rpk                   | FBV      |     10/10     | TESTED        |
| Parks Rpk                   | TwoSum   |     0/10      | TESTED        |
| Reginald Juance             | FBV      |     6/10      | TESTED        |
| Reginald Juance             | TwoSum   |     10/10     | TESTED        |
| Rijaa Zaidi                 | FBV      |     6/10      | TESTED        |
| Rijaa Zaidi                 | TwoSum   |     10/10     | TESTED        |
| Ryan Martin                 | FBV      |     10/10     | TESTED        |
| Ryan Martin                 | TwoSum   |     10/10     | TESTED        |
| Ryan Tsui                   | FBV      |  PSEUDOCODE   | PSEUDOCODE    |
| Ryan Tsui                   | TwoSum   |  PSEUDOCODE   | PSEUDOCODE    |
| Ryan Zhang                  | FBV      |     10/10     | TESTED        |
| Ryan Zhang                  | TwoSum   |     10/10     | TESTED        |
| Samuel Halsband             | FBV      |     10/10     | TESTED        |
| Samuel Halsband             | TwoSum   |     10/10     | TESTED        |
| Santiago Zuluaga            | FBV      |     0/10      | TESTED        |
| Santiago Zuluaga            | TwoSum   |     10/10     | TESTED        |
| Shunyi Chen                 | FBV      |     10/10     | TESTED        |
| Shunyi Chen                 | FBV      | NO SUBMISSION | NO SUBMISSION |
| Shunyi Chen                 | TwoSum   |     9/10      | TESTED        |
| Shunyi Chen                 | TwoSum   | NO SUBMISSION | NO SUBMISSION |
| Solomon Coverdale           | FBV      |     10/10     | TESTED        |
| Solomon Coverdale           | TwoSum   |     9/10      | TESTED        |
| Stephania Calin             | FBV      |     10/10     | TESTED        |
| Stephania Calin             | TwoSum   |     1/10      | TESTED        |
| Sude Genc                   | FBV      |     10/10     | TESTED        |
| Sude Genc                   | TwoSum   |     0/10      | TESTED        |
| Tianna Balkam               | FBV      |     10/10     | TESTED        |
| Tiffany Lin                 | FBV      |     0/10      | TESTED        |
| Treyson Thelusma            | FBV      |     10/10     | TESTED        |
| Treyson Thelusma            | TwoSum   |     10/10     | TESTED        |
| Varun Karamchandani         | FBV      |     10/10     | TESTED        |
| Varun Karamchandani         | TwoSum   |     10/10     | TESTED        |
| Ved Patel                   | TwoSum   |     0/10      | TESTED        |
| Vikram Minhas               | FBV      |     0/10      | TESTED        |
| Vikram Minhas               | TwoSum   |     0/10      | TESTED        |
| William Connors             | FBV      |     0/10      | TESTED        |
| William Connors             | TwoSum   |     0/10      | TESTED        |
| William Conroy              | FBV      |     10/10     | TESTED        |
| William Conroy              | TwoSum   |     0/10      | TESTED        |
| Xinlin Wu                   | FBV      |     8/10      | TESTED        |
| Zhi Xiong Lu                | FBV      |  PSEUDOCODE   | PSEUDOCODE    |
| Zhi Xiong Lu                | TwoSum   |  PSEUDOCODE   | PSEUDOCODE    |

---

## Per-Student Details

### Abdoul Razakou Mahaman Sani — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                              |
| --- | ------------------------ | :-----: | --------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | ERROR: TypeError: object of type 'int' has no len() |
| 2   | `bad=last n=5`           | ❌ Fail | ERROR: TypeError: object of type 'int' has no len() |
| 3   | `bad=middle n=5`         | ❌ Fail | ERROR: TypeError: object of type 'int' has no len() |
| 4   | `single version n=1`     | ❌ Fail | ERROR: TypeError: object of type 'int' has no len() |
| 5   | `n=2 first bad`          | ❌ Fail | ERROR: TypeError: object of type 'int' has no len() |
| 6   | `n=2 second bad`         | ❌ Fail | ERROR: TypeError: object of type 'int' has no len() |
| 7   | `large n bad=1`          | ❌ Fail | ERROR: TypeError: object of type 'int' has no len() |
| 8   | `large n bad=last`       | ❌ Fail | ERROR: TypeError: object of type 'int' has no len() |
| 9   | `large n bad=mid`        | ❌ Fail | ERROR: TypeError: object of type 'int' has no len() |
| 10  | `bad=second-to-last n=5` | ❌ Fail | ERROR: TypeError: object of type 'int' has no len() |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 0
        right = len(n)-1
        while left != right:
            midpoint = (left+right)//2
            if isBadVersion(midpoint):
                right = midpoint
            else:
                left = midpoint+1
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 0
        right = len(n)-1
        while left != right:
            midpoint = (left+right)//2
            if isBadVersion(midpoint):
                right = midpoint
            else:
                left = midpoint+1
        return left
```

</details>

---

### Abdoul Razakou Mahaman Sani — TwoSum &nbsp;`9/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK                                     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                     |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                     |
| 7   | `large target t=700`              | ✅ Pass | OK                                     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                     |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                     |
| 10  | `100-element array t=100`         | ❌ Fail | got [0, 98], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        for i,j in enumerate(nums):
            for k in range(i+1,len(nums)):
                if j + nums[k] == target:
                    return (i,k)
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        for i,j in enumerate(nums):
            for k in range(i+1,len(nums)):
                if j + nums[k] == target:
                    return (i,k)
```

</details>

---

### Albert Chen — FBV &nbsp;`6/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                      |
| --- | ------------------------ | :-----: | ------------------------------------------- |
| 1   | `bad=first n=5`          | ✅ Pass | OK                                          |
| 2   | `bad=last n=5`           | ❌ Fail | got 5.5, expected 5                         |
| 3   | `bad=middle n=5`         | ✅ Pass | OK                                          |
| 4   | `single version n=1`     | ✅ Pass | OK                                          |
| 5   | `n=2 first bad`          | ✅ Pass | OK                                          |
| 6   | `n=2 second bad`         | ❌ Fail | got 2.5, expected 2                         |
| 7   | `large n bad=1`          | ✅ Pass | OK                                          |
| 8   | `large n bad=last`       | ❌ Fail | got 1000000000.1373549, expected 1000000000 |
| 9   | `large n bad=mid`        | ❌ Fail | got 500000000.63735485, expected 500000000  |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK                                          |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution(object):
    def firstBadVersion(self, n):
        left = 1
        right = n
        while left<right:
            midPoint = (left+right)/2
            if isBadVersion(midPoint) == True:
                right = midPoint
            else:
                left = midPoint+1
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution(object):
    def firstBadVersion(self, n):
        left = 1
        right = n
        while left<right:
            midPoint = (left+right)/2
            if isBadVersion(midPoint) == True:
                right = midPoint
            else:
                left = midPoint+1
        return left
```

</details>

---

### Albert Chen — TwoSum &nbsp;`8/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                      |
| --- | --------------------------------- | :-----: | ----------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                                          |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                                          |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | RUNTIME ERROR: TypeError: 'NoneType' object is not iterable |
| 4   | `all negatives t=-8`              | ✅ Pass | OK                                                          |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                                          |
| 6   | `zeros t=0`                       | ❌ Fail | RUNTIME ERROR: TypeError: 'NoneType' object is not iterable |
| 7   | `large target t=700`              | ✅ Pass | OK                                                          |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                                          |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                                          |
| 10  | `100-element array t=100`         | ✅ Pass | OK                                                          |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
         hashmap={}
         for i in range(len(nums)):
             complement = target - nums[i]
             hashmap[nums[i]] = i
             if complement in hashmap and hashmap[complement] !=i:
                 return[i,hashmap[complement]]
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
         hashmap={}
         for i in range(len(nums)):
             complement = target - nums[i]
             hashmap[nums[i]] = i
             if complement in hashmap and hashmap[complement] !=i:
                 return[i,hashmap[complement]]
```

</details>

---

### Alison Batz — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                  |
| --- | ------------------------ | :-----: | --------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 14) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 14) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 14) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 14) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 14) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 14) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 14) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 14) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 14) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 14) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
		#two pointers question

               #variables
               L = 0    #left
               R = n    #right

           while (L < R):
               mid = L + R // 2

             if isBadVersion(mid):
                right = mid

              else:
                 left = mid + 1

         return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
		#two pointers question

               #variables
               L = 0    #left
               R = n    #right

           while (L < R):
               mid = L + R // 2

             if isBadVersion(mid):
                right = mid

              else:
                 left = mid + 1

         return left
```

</details>

---

### Alison Batz — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                             |
| --- | --------------------------------- | :-----: | -------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 19) |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 19) |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 19) |
| 4   | `all negatives t=-8`              | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 19) |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 19) |
| 6   | `zeros t=0`                       | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 19) |
| 7   | `large target t=700`              | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 19) |
| 8   | `pair at end t=9`                 | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 19) |
| 9   | `two-element array t=10`          | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 19) |
| 10  | `100-element array t=100`         | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 19) |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:

          #Variables
            L= 0
            R = L + 1

            length = len(num)

           #initialize hashset

            indices = { }

            while (R < length):
               result = num[L] + num[R] #adding actual value

               if result > target or result < target:
                  L++
                  R++
               else:
                  indices.add(L)
                  indices.add(R)
                  break
     return indices
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:

          #Variables
            L= 0
            R = L + 1

            length = len(num)

           #initialize hashset

            indices = { }

            while (R < length):
               result = num[L] + num[R] #adding actual value

               if result > target or result < target:
                  L++
                  R++
               else:
                  indices.add(L)
                  indices.add(R)
                  break
     return indices
```

</details>

---

### Andrew McBean — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n

        while left <= right:
            mid = (left + right) // 2

            if isBadVersion(mid):
                right = mid - 1
            else:
                left = mid + 1

        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n

        while left <= right:
            mid = (left + right) // 2

            if isBadVersion(mid):
                right = mid - 1
            else:
                left = mid + 1

        return left
```

</details>

---

### Andrew McBean — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums, target):
        seen = {}

        for i in range(len(nums)):
            complement = target - nums[i]

            if complement in seen:
                return [seen[complement], i]

            seen[nums[i]] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums, target):
        seen = {}

        for i in range(len(nums)):
            complement = target - nums[i]

            if complement in seen:
                return [seen[complement], i]

            seen[nums[i]] = i
```

</details>

---

### Ashley Carozza — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:

class Solution:
	def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

        while left < right:
            mid = (left + right) // 2

            if isBadVersion(mid):
                # mid might be the first bad, don't skip it!!!
                right = mid

            else:
                # mid is good, first bad must be later on
                left = mid + 1

        # left == right == first bad version
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.

class Solution:
	def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

        while left < right:
            mid = (left + right) // 2

            if isBadVersion(mid):
                # mid might be the first bad, don't skip it!!!
                right = mid

            else:
                # mid is good, first bad must be later on
                left = mid + 1

        # left == right == first bad version
        return left
```

</details>

---

### Ashley Carozza — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        num_to_index = {}

        for i in range(len(nums)):
            difference = target - nums[i]
            if difference in num_to_index:
                return [num_to_index[difference], i]
            num_to_index[nums[i]] = i

        return []
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        num_to_index = {}

        for i in range(len(nums)):
            difference = target - nums[i]
            if difference in num_to_index:
                return [num_to_index[difference], i]
            num_to_index[nums[i]] = i

        return []
```

</details>

---

### Ava Attina — FBV &nbsp;`1/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                      |
| --- | ------------------------ | :-----: | ------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | ERROR: NameError: name 'mid' is not defined |
| 2   | `bad=last n=5`           | ❌ Fail | ERROR: NameError: name 'mid' is not defined |
| 3   | `bad=middle n=5`         | ❌ Fail | ERROR: NameError: name 'mid' is not defined |
| 4   | `single version n=1`     | ✅ Pass | OK                                          |
| 5   | `n=2 first bad`          | ❌ Fail | ERROR: NameError: name 'mid' is not defined |
| 6   | `n=2 second bad`         | ❌ Fail | ERROR: NameError: name 'mid' is not defined |
| 7   | `large n bad=1`          | ❌ Fail | ERROR: NameError: name 'mid' is not defined |
| 8   | `large n bad=last`       | ❌ Fail | ERROR: NameError: name 'mid' is not defined |
| 9   | `large n bad=mid`        | ❌ Fail | ERROR: NameError: name 'mid' is not defined |
| 10  | `bad=second-to-last n=5` | ❌ Fail | ERROR: NameError: name 'mid' is not defined |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
		#Write your answer here
		leftIndex = 1
		rightIndex = n
		while rightIndex > leftIndex:
			middleIndex = rightIndex - leftIndex
			middleIndex = middleIndex + leftIndex

			if (isBadVersion(mid) == True):
				rightIndex = middleIndex
			else:
				leftIndex = middleIndex +1

		return leftIndex
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
		#Write your answer here
		leftIndex = 1
		rightIndex = n
		while rightIndex > leftIndex:
			middleIndex = rightIndex - leftIndex
			middleIndex = middleIndex + leftIndex

			if (isBadVersion(mid) == True):
				rightIndex = middleIndex
			else:
				leftIndex = middleIndex +1

		return leftIndex
```

</details>

---

### Ava Attina — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                            |
| --- | --------------------------------- | :-----: | --------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | RUNTIME ERROR: KeyError: (7, 0)   |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | RUNTIME ERROR: KeyError: (3, 0)   |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | RUNTIME ERROR: KeyError: (3, 0)   |
| 4   | `all negatives t=-8`              | ❌ Fail | RUNTIME ERROR: KeyError: (-7, 0)  |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | RUNTIME ERROR: KeyError: (3, 0)   |
| 6   | `zeros t=0`                       | ❌ Fail | RUNTIME ERROR: KeyError: (0, 0)   |
| 7   | `large target t=700`              | ❌ Fail | RUNTIME ERROR: KeyError: (600, 0) |
| 8   | `pair at end t=9`                 | ❌ Fail | RUNTIME ERROR: KeyError: (8, 0)   |
| 9   | `two-element array t=10`          | ❌ Fail | RUNTIME ERROR: KeyError: (9, 0)   |
| 10  | `100-element array t=100`         | ❌ Fail | RUNTIME ERROR: KeyError: (99, 0)  |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		#Write your answer here
		checkedIndicies = {}
		for i, num in enumerate(nums):
			value = target - num
			if value in checkedIndicies:
				return [checkedIndicies[value], i]
			checkedIndicies[value, i]
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		#Write your answer here
		checkedIndicies = {}
		for i, num in enumerate(nums):
			value = target - num
			if value in checkedIndicies:
				return [checkedIndicies[value], i]
			checkedIndicies[value, i]
```

</details>

---

### Brian Chen — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Wrapped bare `def firstBadVersion(...)` in `class Solution:`.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
def firstBadVersion(self, n: int) -> int:
    left = 1
    right = n

    while left < right:
        mid = left + (right - left) // 2

        if isBadVersion(mid):
            right = mid
        else:
            left = mid + 1
    return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

        while left < right:
            mid = left + (right - left) // 2

            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

---

### Brian Chen — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                                                    |
| --- | --------------------------------- | :-----: | ----------------------------------------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 4   | `all negatives t=-8`              | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 6   | `zeros t=0`                       | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 7   | `large target t=700`              | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 8   | `pair at end t=9`                 | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 9   | `two-element array t=10`          | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 10  | `100-element array t=100`         | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |

**Edits made by the grader:**

1. Wrapped bare `def twoSum(...)` in `class Solution:` so it matches the expected `Solution().twoSum(...)` call interface.
2. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
def twoSum(nums, target):
    map = {}
    for i in range(len(nums)):
        currentVal = nums[i]
        diff = target - currentVal

        if diff in map:
            return [map[diff], i]

        map[currentVal] = i

nums = [2,7,11,15]
print(twoSum(nums,9))
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(nums, target):
        map = {}
        for i in range(len(nums)):
            currentVal = nums[i]
            diff = target - currentVal

            if diff in map:
                return [map[diff], i]

            map[currentVal] = i

    nums = [2,7,11,15]
    print(twoSum(nums,9))
```

</details>

---

### Cheong Ting Eland Chan — FBV &nbsp;`8/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                            |
| --- | ------------------------ | :-----: | --------------------------------- |
| 1   | `bad=first n=5`          | ✅ Pass | OK                                |
| 2   | `bad=last n=5`           | ✅ Pass | OK                                |
| 3   | `bad=middle n=5`         | ✅ Pass | OK                                |
| 4   | `single version n=1`     | ✅ Pass | OK                                |
| 5   | `n=2 first bad`          | ✅ Pass | OK                                |
| 6   | `n=2 second bad`         | ✅ Pass | OK                                |
| 7   | `large n bad=1`          | ✅ Pass | OK                                |
| 8   | `large n bad=last`       | ✅ Pass | OK                                |
| 9   | `large n bad=mid`        | ❌ Fail | got 499999999, expected 500000000 |
| 10  | `bad=second-to-last n=5` | ❌ Fail | got 3, expected 4                 |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:

    def firstBadVersion(self, n: int) -> int:
        r = n
        l = 0
        highest_good = 0

        while l < r:

            mid = (r + l) // 2

            if isBadVersion(mid):
                r = mid - 1
            else:
                highest_good = max(mid, highest_good)
                l = mid + 1

        return highest_good + 1
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:

    def firstBadVersion(self, n: int) -> int:
        r = n
        l = 0
        highest_good = 0

        while l < r:

            mid = (r + l) // 2

            if isBadVersion(mid):
                r = mid - 1
            else:
                highest_good = max(mid, highest_good)
                l = mid + 1

        return highest_good + 1
```

</details>

---

### Cheong Ting Eland Chan — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                                                    |
| --- | --------------------------------- | :-----: | ----------------------------------------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 4   | `all negatives t=-8`              | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 6   | `zeros t=0`                       | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 7   | `large target t=700`              | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 8   | `pair at end t=9`                 | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 9   | `two-element array t=10`          | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |
| 10  | `100-element array t=100`         | ❌ Fail | RUNTIME ERROR: TypeError: Solution.twoSum() takes 2 positional arguments but 3 were given |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:

  def twoSum(nums: List[int], target: int) -> List[int]:
      seen = {}
      for i, num in enumerate(nums):

          if seen.get(target - num) != None:
              return [seen.get(target - num), i]

          seen[num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:

  def twoSum(nums: List[int], target: int) -> List[int]:
      seen = {}
      for i, num in enumerate(nums):

          if seen.get(target - num) != None:
              return [seen.get(target - num), i]

          seen[num] = i
```

</details>

---

### Christian Zuniga — FBV &nbsp;`—`

**Status:** `NO SUBMISSION`

> ⚠️ Student did not submit an answer.

---

### Christian Zuniga — TwoSum &nbsp;`—`

**Status:** `NO SUBMISSION`

> ⚠️ Student did not submit an answer.

---

### Chuyao Yu — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
# The isBadVersion API is already defined for you.
# def isBadVersion(version: int) -> bool:

class Solution:
    def firstBadVersion(self, n: int) -> int:
        lo, hi = 1, n
        while lo < hi:
            mid = lo + (hi - lo) // 2  # avoids overflow
            if isBadVersion(mid):
                hi = mid       # mid could be the first bad, search left
            else:
                lo = mid + 1   # mid is good, first bad must be to the right
        return lo
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

# The isBadVersion API is already defined for you.

class Solution:
    def firstBadVersion(self, n: int) -> int:
        lo, hi = 1, n
        while lo < hi:
            mid = lo + (hi - lo) // 2  # avoids overflow
            if isBadVersion(mid):
                hi = mid       # mid could be the first bad, search left
            else:
                lo = mid + 1   # mid is good, first bad must be to the right
        return lo
```

</details>

---

### Chuyao Yu — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}  # value -> index
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}  # value -> index
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

---

### Clare Calandra — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
#Notes: use a binary search & think of BST
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left, right = 1, n
		while left < right:
			mid = left + (right - left) // 2

			if isBadVersion(mid):
				right = mid
			else:
				left = mid + 1
		return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
#Notes: use a binary search & think of BST
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left, right = 1, n
		while left < right:
			mid = left + (right - left) // 2

			if isBadVersion(mid):
				right = mid
			else:
				left = mid + 1
		return left
```

</details>

---

### Danila Safronov — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

        while left < right:
            mid = (left + right) // 2

            if isBadVersion(mid):
                # mid could be the first bad version
                right = mid
            else:
                # first bad version must be after mid
                left = mid + 1

        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

        while left < right:
            mid = (left + right) // 2

            if isBadVersion(mid):
                # mid could be the first bad version
                right = mid
            else:
                # first bad version must be after mid
                left = mid + 1

        return left
```

</details>

---

### Danila Safronov — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums, target):
        # Dictionary to store value -> index
        seen = {}

        # Iterate through the list
        for i in range(len(nums)):
            current = nums[i]
            complement = target - current

            # Check if complement already exists
            if complement in seen:
                return [seen[complement], i]

            # Otherwise store current number with its index
            seen[current] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums, target):
        # Dictionary to store value -> index
        seen = {}

        # Iterate through the list
        for i in range(len(nums)):
            current = nums[i]
            complement = target - current

            # Check if complement already exists
            if complement in seen:
                return [seen[complement], i]

            # Otherwise store current number with its index
            seen[current] = i
```

</details>

---

### Dominic Vega — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
        leftptr = 1;
        rightptr = n;
        while(leftptr <= rightptr):
            middle = leftptr + ((rightptr - leftptr) // 2);
            if isBadVersion(middle):
                if (middle == 0 or isBadVersion(middle-1) == False):
                    return middle;
                else:
                    rightptr = middle-1;
            else:
                leftptr = middle+1;
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
        leftptr = 1;
        rightptr = n;
        while(leftptr <= rightptr):
            middle = leftptr + ((rightptr - leftptr) // 2);
            if isBadVersion(middle):
                if (middle == 0 or isBadVersion(middle-1) == False):
                    return middle;
                else:
                    rightptr = middle-1;
            else:
                leftptr = middle+1;
```

</details>

---

### Dominic Vega — TwoSum &nbsp;`8/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                      |
| --- | --------------------------------- | :-----: | ----------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                                          |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                                          |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                                          |
| 4   | `all negatives t=-8`              | ❌ Fail | RUNTIME ERROR: TypeError: 'NoneType' object is not iterable |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | RUNTIME ERROR: TypeError: 'NoneType' object is not iterable |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                                          |
| 7   | `large target t=700`              | ✅ Pass | OK                                                          |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                                          |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                                          |
| 10  | `100-element array t=100`         | ✅ Pass | OK                                                          |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		index = 0;
		hashtable = dict();
		while (index < len(nums)):
		    if nums[index] > target:
		        index += 1;
		        continue;
		    if target - nums[index] in hashtable:
		        return [hashtable[target - nums[index]], index]
		    else:
		        hashtable[nums[index]] = index;
		        index+=1;
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		index = 0;
		hashtable = dict();
		while (index < len(nums)):
		    if nums[index] > target:
		        index += 1;
		        continue;
		    if target - nums[index] in hashtable:
		        return [hashtable[target - nums[index]], index]
		    else:
		        hashtable[nums[index]] = index;
		        index+=1;
```

</details>

---

### Elaine Zou — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
		l, r = 1, n
                while l <= r:
                     mid = (r + l) // 2
                     temp = isBadVersion(mid)
                     if temp and (mid == 1 or not isBadVersion(mid - 1)) :
                          return mid
                     elif not temp :
                          l = mid + 1
                     else:
                          r = mid - 1
         return -1
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
		l, r = 1, n
                while l <= r:
                     mid = (r + l) // 2
                     temp = isBadVersion(mid)
                     if temp and (mid == 1 or not isBadVersion(mid - 1)) :
                          return mid
                     elif not temp :
                          l = mid + 1
                     else:
                          r = mid - 1
         return -1
```

</details>

---

### Elaine Zou — TwoSum &nbsp;`9/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK                                     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                     |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                     |
| 7   | `large target t=700`              | ✅ Pass | OK                                     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                     |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                     |
| 10  | `100-element array t=100`         | ❌ Fail | got [0, 98], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
                difference = {}
                for i, val in enumerate(nums):
                     diff = target - val
                     if diff in nums and nums.index(diff) != i:
                           return [i, nums.index(diff)]
                     difference[val] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
                difference = {}
                for i, val in enumerate(nums):
                     diff = target - val
                     if diff in nums and nums.index(diff) != i:
                           return [i, nums.index(diff)]
                     difference[val] = i
```

</details>

---

### Ethan Wong — FBV &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
low = 1
high = n
while low <= high:
  middle = int((low - high) / 2 + low)
  if isBadVersion(middle):
    if low == high:
        return low
     high = mid - 1
  else //when bad version is greater than low
    low = mid + 1

return -1
```

</details>

---

### Ethan Wong — TwoSum &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
left = 0
right = length of nums
while left is less than right:
  if sum of left and right num is greater than target:
    right--
  else if sum of left and right num is less than target:
    left++
  else
    return true
return false
```

</details>

---

### Evan Weisberg — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 0
		right = n
		while left <= right:
			adjustMid = left+(right-left)//2
			if isBadVersion(adjustMid):
				right = adjustMid - 1
			else:
				left = adjustMid + 1
		return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 0
		right = n
		while left <= right:
			adjustMid = left+(right-left)//2
			if isBadVersion(adjustMid):
				right = adjustMid - 1
			else:
				left = adjustMid + 1
		return left
```

</details>

---

### Evan Weisberg — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seenhash = {}
        for i, n in enumerate(nums):
            comp = target - n
            if comp in seenhash:
                return [seenhash[comp],i]
            seenhash[n] = i
        return null
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seenhash = {}
        for i, n in enumerate(nums):
            comp = target - n
            if comp in seenhash:
                return [seenhash[comp],i]
            seenhash[n] = i
        return null
```

</details>

---

### Gabriel Yang — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                            |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 6 (<student>, line 9) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 6 (<student>, line 9) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 6 (<student>, line 9) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 6 (<student>, line 9) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 6 (<student>, line 9) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 6 (<student>, line 9) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 6 (<student>, line 9) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 6 (<student>, line 9) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 6 (<student>, line 9) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 6 (<student>, line 9) |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
		#Write your answer here

l = 1
r = n

while l < r:
     mid = (l + r) // 2
     if isBadVersion(mid):
          r = mid
     else:
          l = mid + 1

return l
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
		#Write your answer here

l = 1
r = n

while l < r:
     mid = (l + r) // 2
     if isBadVersion(mid):
          r = mid
     else:
          l = mid + 1

return l
```

</details>

---

### Gabriel Yang — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		#Write your answer here

               hset = {}

               for i in range(len(nums)):
                   complement = target - nums[i]
                   if complement in hset:
                       return [i, hset[complement]]

                   hset[nums[i]] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		#Write your answer here

               hset = {}

               for i in range(len(nums)):
                   complement = target - nums[i]
                   if complement in hset:
                       return [i, hset[complement]]

                   hset[nums[i]] = i
```

</details>

---

### Gaven Chan — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n
        while left < right:
            mid = left + (right - left) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1

        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n
        while left < right:
            mid = left + (right - left) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1

        return left
```

</details>

---

### Gaven Chan — TwoSum &nbsp;`9/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK                                     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                     |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                     |
| 7   | `large target t=700`              | ✅ Pass | OK                                     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                     |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                     |
| 10  | `100-element array t=100`         | ❌ Fail | got [0, 98], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            num1 = nums[i]
            for j in range(i + 1,len(nums)):
                num2 = nums[j]
                if(num1 + num2 == target):
                    return i,j
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            num1 = nums[i]
            for j in range(i + 1,len(nums)):
                num2 = nums[j]
                if(num1 + num2 == target):
                    return i,j
```

</details>

---

### Gavin Ng — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
        l = 1
        r = n

	    while l < r:
            m = (r + l) // 2
            if isBadVersion(m):
                r = m
            else:
                l = m + 1
        return l
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
        l = 1
        r = n

	    while l < r:
            m = (r + l) // 2
            if isBadVersion(m):
                r = m
            else:
                l = m + 1
        return l
```

</details>

---

### Gavin Ng — TwoSum &nbsp;`9/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK                                     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                     |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                     |
| 7   | `large target t=700`              | ✅ Pass | OK                                     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                     |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                     |
| 10  | `100-element array t=100`         | ❌ Fail | got [0, 98], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            for x in range(i + 1, len(nums)):
                if nums[i] + nums[x] == target:
                    return [i, x]
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            for x in range(i + 1, len(nums)):
                if nums[i] + nums[x] == target:
                    return [i, x]
```

</details>

---

### Hewitt Wang — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                          |
| --- | ------------------------ | :-----: | ----------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: expected '(' (<student>, line 7) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: expected '(' (<student>, line 7) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: expected '(' (<student>, line 7) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: expected '(' (<student>, line 7) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: expected '(' (<student>, line 7) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: expected '(' (<student>, line 7) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: expected '(' (<student>, line 7) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: expected '(' (<student>, line 7) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: expected '(' (<student>, line 7) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: expected '(' (<student>, line 7) |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
# pseudocode

def isBad: int -> boolean;

let vs = list of version numbers

// being a 'bad version' is a monotone predicate over a sorted domain, thus we can binar y search over the predicate

let lo = 0, hi = length of vs

let lb = 0

# note: // is floor division
while lo /= hi do {
   m := lo + (hi - lo) // 2
   v := vs[m]
   if isBad(v) {
      lb = v;
      hi = m;
   } else {
      lo = m + 1;
   }
}

ret lb
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

# pseudocode

def isBad: int -> boolean;

let vs = list of version numbers

// being a 'bad version' is a monotone predicate over a sorted domain, thus we can binar y search over the predicate

let lo = 0, hi = length of vs

let lb = 0

# note: // is floor division
while lo /= hi do {
   m := lo + (hi - lo) // 2
   v := vs[m]
   if isBad(v) {
      lb = v;
      hi = m;
   } else {
      lo = m + 1;
   }
}

ret lb
```

</details>

---

### Hewitt Wang — TwoSum &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
# pseudocode

// complement -> index
let map = {}
let T be the target sum

for n, i in enumerate(nums) {
    if map[n] exists
       return [i, map[n]]
    else
      map[T - n] = i
}
```

</details>

---

### Ian Porto — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
               start = 1
               end = n
               while start < end:
                      sum = start + end
                      middle = sum // 2
                      if isBadVersion(middle):
                          end = middle
                      else:
                          start = middle + 1
               return start
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
               start = 1
               end = n
               while start < end:
                      sum = start + end
                      middle = sum // 2
                      if isBadVersion(middle):
                          end = middle
                      else:
                          start = middle + 1
               return start
```

</details>

---

### Ian Porto — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
               seen = {}
               for i,a in enumerate(nums):
                    b = target - a
                    if b in seen:
                         return [seen[b], i]
                    seen[a] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
               seen = {}
               for i,a in enumerate(nums):
                    b = target - a
                    if b in seen:
                         return [seen[b], i]
                    seen[a] = i
```

</details>

---

### Isabella Yang — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n
        while left < right:
            mid = (left + right) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n
        while left < right:
            mid = (left + right) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

---

### Isabella Yang — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        dictionary = {}
        for i, numbers in enumerate(nums):
            remainder = target - numbers
            if remainder in dictionary:
                return [dictionary[remainder], i]
            else:
                dictionary[numbers] =  i
        return 0
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        dictionary = {}
        for i, numbers in enumerate(nums):
            remainder = target - numbers
            if remainder in dictionary:
                return [dictionary[remainder], i]
            else:
                dictionary[numbers] =  i
        return 0
```

</details>

---

### Ivan Cheung — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                  |
| --- | ------------------------ | :-----: | --------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 19) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 19) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 19) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 19) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 19) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 19) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 19) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 19) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 19) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 19) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
               left = 1
               right = n

               while left < right:
                      mid = (left + right) // 2

                      if isBadVersion(mid):
                             right = mid
                      else:
                             left = mid + 1

                return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
               left = 1
               right = n

               while left < right:
                      mid = (left + right) // 2

                      if isBadVersion(mid):
                             right = mid
                      else:
                             left = mid + 1

                return left
```

</details>

---

### Ivan Cheung — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                                                |
| --- | --------------------------------- | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 4   | `all negatives t=-8`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 6   | `zeros t=0`                       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 7   | `large target t=700`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 8   | `pair at end t=9`                 | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 9   | `two-element array t=10`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 10  | `100-element array t=100`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		visited = {}

                for i, num in enumerate(nums):
                       neededVal = target - num

                       if neededVal in visited:
                              return [visited[neededVal], i]

                       visited[num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		visited = {}

                for i, num in enumerate(nums):
                       neededVal = target - num

                       if neededVal in visited:
                              return [visited[neededVal], i]

                       visited[num] = i
```

</details>

---

### Jack Stevens — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
  def firstBadVersion(self, n: int) -> int:
    l = 0
    r = n
    while(l < r):
      m = (l + r) // 2
      if(isBadVersion(m)):
        r = m
      else:
        l = m + 1
    return l
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
  def firstBadVersion(self, n: int) -> int:
    l = 0
    r = n
    while(l < r):
      m = (l + r) // 2
      if(isBadVersion(m)):
        r = m
      else:
        l = m + 1
    return l
```

</details>

---

### Jack Stevens — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
  def twoSum(self, nums: List[int], target: int) -> List[int]:
    checked = {}
    for i in range(len(nums)):
      if target - nums[i] in checked:
        return [checked[target - nums[i]], i]
      checked[nums[i]] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
  def twoSum(self, nums: List[int], target: int) -> List[int]:
    checked = {}
    for i in range(len(nums)):
      if target - nums[i] in checked:
        return [checked[target - nums[i]], i]
      checked[nums[i]] = i
```

</details>

---

### Jake Steck — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        low = 1
        high = n
        result = n

        while low <= high:
            mid = low + (high - low) // 2
            if isBadVersion(mid):
                result = mid
                high = mid - 1
            else:
                low = mid + 1
        return result
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        low = 1
        high = n
        result = n

        while low <= high:
            mid = low + (high - low) // 2
            if isBadVersion(mid):
                result = mid
                high = mid - 1
            else:
                low = mid + 1
        return result
```

</details>

---

### Jake Steck — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        prevMap = {}

        for i, n in enumerate(nums):
            diff = target - n
            if diff in prevMap:
                return [prevMap[diff], i]
            prevMap[n] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        prevMap = {}

        for i, n in enumerate(nums):
            diff = target - n
            if diff in prevMap:
                return [prevMap[diff], i]
            prevMap[n] = i
```

</details>

---

### Jason Guo — FBV &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
left = 1
right = n

while left < right:
    middle = (left + right) / 2

    if isBadVersion(middle) is true:
        right = middl
    else:
        left = middle + 1
return left
```

</details>

---

### Jason Guo — TwoSum &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
hashmap = empty hashmap

for each index i in nums:
    difference = target - nums[i]
    if difference is in hashhmap:
        return [hashmap[differenc], i]
    hashmap[nums[i]] = i
```

</details>

---

### Jason Seng — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                           |
| --- | ------------------------ | :-----: | ------------------------------------------------ |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: expected ':' (<student>, line 11) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: expected ':' (<student>, line 11) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: expected ':' (<student>, line 11) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: expected ':' (<student>, line 11) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: expected ':' (<student>, line 11) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: expected ':' (<student>, line 11) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: expected ':' (<student>, line 11) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: expected ':' (<student>, line 11) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: expected ':' (<student>, line 11) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: expected ':' (<student>, line 11) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
		right = n

		while left < right
			mid = (left + right) // 2

			if isBadVersion(mid):
				right = mid
			else:
				left = mid + 1
		return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
		right = n

		while left < right
			mid = (left + right) // 2

			if isBadVersion(mid):
				right = mid
			else:
				left = mid + 1
		return left
```

</details>

---

### Jason Seng — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                               |
| --- | --------------------------------- | :-----: | ---------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | RUNTIME ERROR: NameError: name 'lens' is not defined |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | RUNTIME ERROR: NameError: name 'lens' is not defined |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | RUNTIME ERROR: NameError: name 'lens' is not defined |
| 4   | `all negatives t=-8`              | ❌ Fail | RUNTIME ERROR: NameError: name 'lens' is not defined |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | RUNTIME ERROR: NameError: name 'lens' is not defined |
| 6   | `zeros t=0`                       | ❌ Fail | RUNTIME ERROR: NameError: name 'lens' is not defined |
| 7   | `large target t=700`              | ❌ Fail | RUNTIME ERROR: NameError: name 'lens' is not defined |
| 8   | `pair at end t=9`                 | ❌ Fail | RUNTIME ERROR: NameError: name 'lens' is not defined |
| 9   | `two-element array t=10`          | ❌ Fail | RUNTIME ERROR: NameError: name 'lens' is not defined |
| 10  | `100-element array t=100`         | ❌ Fail | RUNTIME ERROR: NameError: name 'lens' is not defined |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		seen = {}

		for i in range (lens(nums)):
			complement = target - nums[i]

			if complement in seen:
				return[seen[complement], i]

			seen[nums[i]] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		seen = {}

		for i in range (lens(nums)):
			complement = target - nums[i]

			if complement in seen:
				return[seen[complement], i]

			seen[nums[i]] = i
```

</details>

---

### Jin Noh — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        low = 1
        high = n

        while low < high:
            mid = low + (high - low) // 2

            if isBadVersion(mid):
                high = mid
            else:
                low = mid + 1

        return low
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        low = 1
        high = n

        while low < high:
            mid = low + (high - low) // 2

            if isBadVersion(mid):
                high = mid
            else:
                low = mid + 1

        return low
```

</details>

---

### Jin Noh — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        seen = {}
        for i in range(len(nums)):
            val = nums[i]
            if (target - val) in seen:
                return [seen[target - val], i]
            seen[val] = i
        return []
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        seen = {}
        for i in range(len(nums)):
            val = nums[i]
            if (target - val) in seen:
                return [seen[target - val], i]
            seen[val] = i
        return []
```

</details>

---

### Jordanny Ramos Rodriguez — FBV &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
Initialize 'low' to 1
Initialize 'high' to n
Initialize 'firstBad' to 0

While 'low' is less than or equal to 'high':

    Calculate 'mid' point:
        mid = low + (high - low) / 2


    If the result is True (Version is Bad):
        Store 'mid' as the current 'firstBad' number
        Adjust 'high' to mid - 1

    Else (Version is Good):
        Adjust 'low' to mid + 1

Return 'firstBad'
```

</details>

---

### Jordanny Ramos Rodriguez — TwoSum &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
Initialize an empty hash map

For each index 'i' and its corresponding 'number' in the array:

    Calculate the 'complement' as: target - number

    If 'complement' exists in the hash map:
        Return a list containing [the index of the complement, current index 'i']

    Else:
        Add the current 'number' to the hash map with its index 'i'
        (Key: number, Value: i)
```

</details>

---

### Juan Moran — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
                right = n
                while left < right:
                     mid = (left+right) / 2
                     if isBadVersion(mid): # Begin at middle
                        right = mid # shift right pointer to mid , search from left to mid
                     else: #increment left ptr to mid
                           left = mid + 1
                 return left # once left reaches right that is the repeat
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
                right = n
                while left < right:
                     mid = (left+right) / 2
                     if isBadVersion(mid): # Begin at middle
                        right = mid # shift right pointer to mid , search from left to mid
                     else: #increment left ptr to mid
                           left = mid + 1
                 return left # once left reaches right that is the repeat
```

</details>

---

### Juan Moran — TwoSum &nbsp;`9/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK                                     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                     |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                     |
| 7   | `large target t=700`              | ✅ Pass | OK                                     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                     |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                     |
| 10  | `100-element array t=100`         | ❌ Fail | got [0, 98], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution: #2 pointer approach
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		for i in range(len(nums)):
                      for j in range(i+1, len(nums)):
                              if (nums[i] + nums[j] == target):
                                   return [i,j]
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution: #2 pointer approach
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		for i in range(len(nums)):
                      for j in range(i+1, len(nums)):
                              if (nums[i] + nums[j] == target):
                                   return [i,j]
```

</details>

---

### Justin Yu — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
        # Identify the bounds
        leftpointer = 1
        rightpointer = n
        lastest_best_version = 0
        while rightpointer >= leftpointer:
            version = (rightpointer+leftpointer)//2
            if isBadVersion(version) == False:
                leftpointer = version + 1
                lastest_best_version = version
            else:
                rightpointer = version - 1
        return lastest_best_version + 1
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
        # Identify the bounds
        leftpointer = 1
        rightpointer = n
        lastest_best_version = 0
        while rightpointer >= leftpointer:
            version = (rightpointer+leftpointer)//2
            if isBadVersion(version) == False:
                leftpointer = version + 1
                lastest_best_version = version
            else:
                rightpointer = version - 1
        return lastest_best_version + 1
```

</details>

---

### Justin Yu — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                                                |
| --- | --------------------------------- | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 4   | `all negatives t=-8`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 6   | `zeros t=0`                       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 7   | `large target t=700`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 8   | `pair at end t=9`                 | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 9   | `two-element array t=10`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 10  | `100-element array t=100`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
        # Define a Hashmap
        PosComp = {}

        # Loop Through the list checking if each compliment is in the Hashmap if not add it
        for num in range(len(nums)):
            Compliment = target - nums[num]
            if Compliment in PosComp:
                return [PosComp[Compliment], num]
            else:
                PosComp[nums[num]] = num
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
        # Define a Hashmap
        PosComp = {}

        # Loop Through the list checking if each compliment is in the Hashmap if not add it
        for num in range(len(nums)):
            Compliment = target - nums[num]
            if Compliment in PosComp:
                return [PosComp[Compliment], num]
            else:
                PosComp[nums[num]] = num
```

</details>

---

### Kartik Wahlin — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        low = 1
        high = n

        bad = isBadVersion(1)
        while low <= high:
            mid = low + int((high-low)/2)
            if mid == 1 and bad:
                return 1
            #print (mid)

            curBad = isBadVersion(mid)
            prevBad = isBadVersion(mid-1)
            if prevBad: # in the red
                high = mid-1

            elif curBad: #splitline
                return mid

            else: # in the white
                low = mid+1
        return -1
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        low = 1
        high = n

        bad = isBadVersion(1)
        while low <= high:
            mid = low + int((high-low)/2)
            if mid == 1 and bad:
                return 1
            #print (mid)

            curBad = isBadVersion(mid)
            prevBad = isBadVersion(mid-1)
            if prevBad: # in the red
                high = mid-1

            elif curBad: #splitline
                return mid

            else: # in the white
                low = mid+1
        return -1
```

</details>

---

### Kartik Wahlin — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        itinerary = {}
        for i,num in enumerate(nums):
            if (target-num) in itinerary:
                return [itinerary[target-num], i]
            if not (num in itinerary):
                itinerary[num] = i
        return []
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        itinerary = {}
        for i,num in enumerate(nums):
            if (target-num) in itinerary:
                return [itinerary[target-num], i]
            if not (num in itinerary):
                itinerary[num] = i
        return []
```

</details>

---

### Kathryn Schauber — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
# The isBadVersion API is already defined for you.
# @param version, an integer
# @return a bool
# def isBadVersion(version):

class Solution(object):
    def firstBadVersion(self, n):
        """
        :type n: int
        :rtype: int
        """
        left = 0
        right = n - 1
        mid = (left + right) // 2
        fbl = 1
        fbr = n

        while fbl != fbr:
            if isBadVersion(mid + 1):
                fbl = mid + 1
                right = mid - 1
            else:
                left = mid + 1
                fbr = mid + 2
            mid = (left + right) // 2
        return fbl
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

# The isBadVersion API is already defined for you.
# @param version, an integer
# @return a bool

class Solution(object):
    def firstBadVersion(self, n):
        """
        :type n: int
        :rtype: int
        """
        left = 0
        right = n - 1
        mid = (left + right) // 2
        fbl = 1
        fbr = n

        while fbl != fbr:
            if isBadVersion(mid + 1):
                fbl = mid + 1
                right = mid - 1
            else:
                left = mid + 1
                fbr = mid + 2
            mid = (left + right) // 2
        return fbl
```

</details>

---

### Kathryn Schauber — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution(object):
    def twoSum(self, nums, target):
        hashmap = {}

        for i in range(len(nums)):
            if target - nums[i] in hashmap:
                return [hashmap[target - nums[i]], i]
            else:
                hashmap[nums[i]] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution(object):
    def twoSum(self, nums, target):
        hashmap = {}

        for i in range(len(nums)):
            if target - nums[i] in hashmap:
                return [hashmap[target - nums[i]], i]
            else:
                hashmap[nums[i]] = i
```

</details>

---

### Kenneth Ng — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        #Write your answer here
        left = 1
        right = n

        while left <= right:
            midpoint = (left + right) // 2
            midIsBad = isBadVersion(midpoint)

            if midIsBad: right = midpoint - 1
            else: left = midpoint + 1

        return left

""" typescript "pseudocode"
function firstBadVersion(n: number): number {
    let left = 1;
    let right = n;

    while (left <= right) {
        const midpoint = Math.floor((left + right) / 2);
        const midIsBad = isBadVersion(midpoint);

        if (midIsBad) right = midpoint - 1;
        else left = midpoint + 1;
    }

    return left;
}
"""
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        #Write your answer here
        left = 1
        right = n

        while left <= right:
            midpoint = (left + right) // 2
            midIsBad = isBadVersion(midpoint)

            if midIsBad: right = midpoint - 1
            else: left = midpoint + 1

        return left

""" typescript "pseudocode"
function firstBadVersion(n: number): number {
    let left = 1;
    let right = n;

    while (left <= right) {
        const midpoint = Math.floor((left + right) / 2);
        const midIsBad = isBadVersion(midpoint);

        if (midIsBad) right = midpoint - 1;
        else left = midpoint + 1;
    }

    return left;
}
"""
```

</details>

---

### Kenneth Ng — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        #Write your answer here
        map = {}

        for i, val in enumerate(nums):
            difference = target - val
            if difference in map: return [map[difference], i]
            map[val] = i;

"""typescript "pseudocode"
function twoSum(nums: number[], target: number): number[] {
    const map = new Map<number, number>();

    for (let i = 0; i < nums.length; i++) {
        const difference = target - nums[i];

        if (map.has(difference)) return [map.get(difference), i];
        map.set(nums[i], i);
    }
}
"""
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        #Write your answer here
        map = {}

        for i, val in enumerate(nums):
            difference = target - val
            if difference in map: return [map[difference], i]
            map[val] = i;

"""typescript "pseudocode"
function twoSum(nums: number[], target: number): number[] {
    const map = new Map<number, number>();

    for (let i = 0; i < nums.length; i++) {
        const difference = target - nums[i];

        if (map.has(difference)) return [map.get(difference), i];
        map.set(nums[i], i);
    }
}
"""
```

</details>

---

### Kushagra Singh — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n
        while left < right:
            mid = left + (right - left) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n
        while left < right:
            mid = left + (right - left) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

---

### Kushagra Singh — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

---

### Kyle Godzki — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left, right = 1, n
                while left < right:
                     mid = left + (right - left) // 2
                     if isBadVersion(mid):
                         right = mid
                     else:
                          left = mid + 1
                return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
		left, right = 1, n
                while left < right:
                     mid = left + (right - left) // 2
                     if isBadVersion(mid):
                         right = mid
                     else:
                          left = mid + 1
                return left
```

</details>

---

### Kyle Godzki — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                                                |
| --- | --------------------------------- | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 4   | `all negatives t=-8`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 6   | `zeros t=0`                       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 7   | `large target t=700`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 8   | `pair at end t=9`                 | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 9   | `two-element array t=10`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 10  | `100-element array t=100`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		hashmap = {}
                for i, num in enumerate(nums):
                     complement = target - num
                     if complement in hashmap:
                          return [hashmap[complement], i]
                     seen[num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		hashmap = {}
                for i, num in enumerate(nums):
                     complement = target - num
                     if complement in hashmap:
                          return [hashmap[complement], i]
                     seen[num] = i
```

</details>

---

### Lilia Diusheyeva — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                 |
| --- | ------------------------ | :-----: | -------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:

class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
                right = n

                while left < right:
                          mid = left + (right - left) //2

                          if isBadVersion(mid):
                                 right = mid
                          else:
                                  left = mid +1

                 return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.

class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
                right = n

                while left < right:
                          mid = left + (right - left) //2

                          if isBadVersion(mid):
                                 right = mid
                          else:
                                  left = mid +1

                 return left
```

</details>

---

### Lilia Diusheyeva — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                                                |
| --- | --------------------------------- | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 4   | `all negatives t=-8`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 6   | `zeros t=0`                       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 7   | `large target t=700`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 8   | `pair at end t=9`                 | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 9   | `two-element array t=10`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |
| 10  | `100-element array t=100`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 6) |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		hashmap = {}

                for i in range(len(nums)):
                       complement = target - nums[i]

                       if complement in hashmap:
                               return [hashmap[complement], i]

                        hashmap[nums[i]] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		hashmap = {}

                for i in range(len(nums)):
                       complement = target - nums[i]

                       if complement in hashmap:
                               return [hashmap[complement], i]

                        hashmap[nums[i]] = i
```

</details>

---

### Lilian Yuan — FBV &nbsp;`1/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                               |
| --- | ------------------------ | :-----: | ------------------------------------ |
| 1   | `bad=first n=5`          | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |
| 2   | `bad=last n=5`           | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |
| 3   | `bad=middle n=5`         | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |
| 4   | `single version n=1`     | ✅ Pass | OK                                   |
| 5   | `n=2 first bad`          | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |
| 6   | `n=2 second bad`         | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |
| 7   | `large n bad=1`          | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |
| 8   | `large n bad=last`       | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |
| 9   | `large n bad=mid`        | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |
| 10  | `bad=second-to-last n=5` | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |

**Edits made by the grader:**

1. Wrapped bare `def firstBadVersion(...)` in `class Solution:`.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
def firstBadVersion(self, n):
    left, right = 1, n
    while left < right:
        mid = (left + right)
        if isBadVersion(mid):
            right = mid
        else:
            left = mid + 1
    return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n):
        left, right = 1, n
        while left < right:
            mid = (left + right)
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

---

### Lilian Yuan — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Wrapped bare `def twoSum(...)` in `class Solution:` so it matches the expected `Solution().twoSum(...)` call interface.
2. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
def twoSum(self, nums, target):
    seen = {}
    for i, num in enumerate(nums):
        complement = target - num
        if complement in seen:
            return [seen[complement], i]
        seen[num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums, target):
        seen = {}
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

---

### Logan Regueiferos — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

        while left < right:
            mid = left + (right - left) // 2

            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

        while left < right:
            mid = left + (right - left) // 2

            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

---

### Logan Regueiferos — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        prev_map = {}

        for i, n in enumerate(nums):
            diff = target - n
            if diff in prev_map:
                return [prev_map[diff], i]
            prev_map[n] = i

        return []
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        prev_map = {}

        for i, n in enumerate(nums):
            diff = target - n
            if diff in prev_map:
                return [prev_map[diff], i]
            prev_map[n] = i

        return []
```

</details>

---

### Maddie Broderick — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left, right = 1, n
		while left < right:
			mid = (left + right) // 2
			if isBadVersion(mid):
				right = mid
			else:
				left = mid + 1
		return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
		left, right = 1, n
		while left < right:
			mid = (left + right) // 2
			if isBadVersion(mid):
				right = mid
			else:
				left = mid + 1
		return left
```

</details>

---

### Maddie Broderick — TwoSum &nbsp;`9/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK                                     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                     |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                     |
| 7   | `large target t=700`              | ✅ Pass | OK                                     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                     |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                     |
| 10  | `100-element array t=100`         | ❌ Fail | got [0, 98], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		hash = {}
		for i in range(len(nums)):
			hash[nums[i]] = i
		for i in range(len(nums)):
			val = target - nums[i]
			if val in hash and hash[val] != i:
				return [i,hash[val]]
		return []
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		hash = {}
		for i in range(len(nums)):
			hash[nums[i]] = i
		for i in range(len(nums)):
			val = target - nums[i]
			if val in hash and hash[val] != i:
				return [i,hash[val]]
		return []
```

</details>

---

### Mahir Majlis — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
                right = n

                while left < right:
                                mid = left + (right - left)/2
                                if isBadVersion(mid): right = mid
				else: left = mid + 1

		return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
                right = n

                while left < right:
                                mid = left + (right - left)/2
                                if isBadVersion(mid): right = mid
				else: left = mid + 1

		return left
```

</details>

---

### Mahir Majlis — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
                nums_dict = dict()
                for i in range(len(nums)):
                        diff = target - nums[i]
                        if diff in nums_dict:
                                return [nums_dict[diff], i]
                        nums_dict[nums[i]] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
                nums_dict = dict()
                for i in range(len(nums)):
                        diff = target - nums[i]
                        if diff in nums_dict:
                                return [nums_dict[diff], i]
                        nums_dict[nums[i]] = i
```

</details>

---

### Matthew Park — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
# The isBadVersion API is already defined for you.
# def isBadVersion(version: int) -> bool:
class Solution:
    def firstBadVersion(self, n: int) -> int:
        #edge case where the 1st version is bad
        if isBadVersion(1):
            return 1

        #bin search
        low = 1
        high = n
        curr = (high // 2)
        if n >= 3:
            while ((high - low) >= 2):#run until size 2, when low's value is 0 and high's value is 1
                if isBadVersion(curr):
                    #change high
                    high = curr
                else:
                    low = curr
                curr = ((high - low) // 2) + low

            return high
        elif n == 2:
            return 2
        return 1 #if n == 1
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

# The isBadVersion API is already defined for you.
class Solution:
    def firstBadVersion(self, n: int) -> int:
        #edge case where the 1st version is bad
        if isBadVersion(1):
            return 1

        #bin search
        low = 1
        high = n
        curr = (high // 2)
        if n >= 3:
            while ((high - low) >= 2):#run until size 2, when low's value is 0 and high's value is 1
                if isBadVersion(curr):
                    #change high
                    high = curr
                else:
                    low = curr
                curr = ((high - low) // 2) + low

            return high
        elif n == 2:
            return 2
        return 1 #if n == 1
```

</details>

---

### Matthew Park — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        #create dictionary
        dic = dict()
        for i, elem in enumerate(nums):
            if (target - elem) in dic:
                return [i, dic[target - elem]]
            else:
                dic[elem] = i#set dictionary element
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        #create dictionary
        dic = dict()
        for i, elem in enumerate(nums):
            if (target - elem) in dic:
                return [i, dic[target - elem]]
            else:
                dic[elem] = i#set dictionary element
```

</details>

---

### Max Stehura — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                  |
| --- | ------------------------ | :-----: | --------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 13) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 13) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 13) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 13) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 13) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 13) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 13) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 13) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 13) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 13) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int):
		l = 1
		r = n
		while l < r:
			mid = l + (r - l) // 2

            if isBadVersion(mid):
                r = mid
			else:
				l = mid + 1

        return l
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int):
		l = 1
		r = n
		while l < r:
			mid = l + (r - l) // 2

            if isBadVersion(mid):
                r = mid
			else:
				l = mid + 1

        return l
```

</details>

---

### Max Stehura — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: list[int], target: int):
		seen = {}

		for i, num1 in enumerate(nums):
			num2 = target - num1

			if num2 in seen:
				return [seen[num2], i]

			seen[num1] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: list[int], target: int):
		seen = {}

		for i, num1 in enumerate(nums):
			num2 = target - num1

			if num2 in seen:
				return [seen[num2], i]

			seen[num1] = i
```

</details>

---

### Michael DiNapoli — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                            |
| --- | ------------------------ | :-----: | ------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 6) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 6) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 6) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 6) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 6) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 6) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 6) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 6) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 6) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: invalid syntax (<student>, line 6) |

**Edits made by the grader:**

1. Wrapped bare `def firstBadVersion(...)` in `class Solution:`.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
Class Solution: def firstBadVersion(self, n):
   left = 1
   right = n

   while left < right:
      mid = (left +right) // 2

      if  isBadVersion(mid):
         right = mid
      else:
         left = mid + 1

return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    Class Solution: def firstBadVersion(self, n):
       left = 1
       right = n

       while left < right:
          mid = (left +right) // 2

          if  isBadVersion(mid):
             right = mid
          else:
             left = mid + 1

    return left
```

</details>

---

### Michael DiNapoli — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                |
| --- | --------------------------------- | :-----: | ----------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | RUNTIME ERROR: NameError: name 'rande' is not defined |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | RUNTIME ERROR: NameError: name 'rande' is not defined |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | RUNTIME ERROR: NameError: name 'rande' is not defined |
| 4   | `all negatives t=-8`              | ❌ Fail | RUNTIME ERROR: NameError: name 'rande' is not defined |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | RUNTIME ERROR: NameError: name 'rande' is not defined |
| 6   | `zeros t=0`                       | ❌ Fail | RUNTIME ERROR: NameError: name 'rande' is not defined |
| 7   | `large target t=700`              | ❌ Fail | RUNTIME ERROR: NameError: name 'rande' is not defined |
| 8   | `pair at end t=9`                 | ❌ Fail | RUNTIME ERROR: NameError: name 'rande' is not defined |
| 9   | `two-element array t=10`          | ❌ Fail | RUNTIME ERROR: NameError: name 'rande' is not defined |
| 10  | `100-element array t=100`         | ❌ Fail | RUNTIME ERROR: NameError: name 'rande' is not defined |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
   def twoSum(self, nums, target):
      seen = {}
      for i in rande(len(nums)):
         current = nums[i]
         needed = target - current
         if needed in seen:
            return [seen[needed], i]

         seen[current] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
   def twoSum(self, nums, target):
      seen = {}
      for i in rande(len(nums)):
         current = nums[i]
         needed = target - current
         if needed in seen:
            return [seen[needed], i]

         seen[current] = i
```

</details>

---

### Monica Gnajewski — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        start = 1
        end = n
        while start < end:
            mid = (start+end) // 2
            if isBadVersion(mid):
                end = mid
            else:
                start = mid + 1
        return start
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        start = 1
        end = n
        while start < end:
            mid = (start+end) // 2
            if isBadVersion(mid):
                end = mid
            else:
                start = mid + 1
        return start
```

</details>

---

### Monica Gnajewski — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        explored = {}
        for i, val in enumerate(nums):
            comp = target - val
            if comp in explored:
                return [explored[comp], i]
            explored[val] = i
        return
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        explored = {}
        for i, val in enumerate(nums):
            comp = target - val
            if comp in explored:
                return [explored[comp], i]
            explored[val] = i
        return
```

</details>

---

### Naman Kukreti — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                        |
| --- | ------------------------ | :-----: | ------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 15) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 15) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 15) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 15) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 15) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 15) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 15) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 15) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 15) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 15) |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
		L = 1
R = n
while L < R:
  mid = (L+R)//2
  if isBadVersion(mid):
    right = mid
  else:
    left = mid+1
return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
		L = 1
R = n
while L < R:
  mid = (L+R)//2
  if isBadVersion(mid):
    right = mid
  else:
    left = mid+1
return left
```

</details>

---

### Naman Kukreti — TwoSum &nbsp;`9/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK                                     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                     |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                     |
| 7   | `large target t=700`              | ✅ Pass | OK                                     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                     |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                     |
| 10  | `100-element array t=100`         | ❌ Fail | got [0, 98], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        dicte = {}
        for i, n in enumerate(nums):
            dicte[n] = i
        for i in range(len(nums)):
            if target-nums[i] in dicte and i != dicte[target-nums[i]]:
                return [i,dicte[target-nums[i]]]
        return []
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        dicte = {}
        for i, n in enumerate(nums):
            dicte[n] = i
        for i in range(len(nums)):
            if target-nums[i] in dicte and i != dicte[target-nums[i]]:
                return [i,dicte[target-nums[i]]]
        return []
```

</details>

---

### Naomi Sellam — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n
        while left < right:
            mid = (left + right) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n
        while left < right:
            mid = (left + right) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

---

### Naomi Sellam — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

---

### Nicholas Friedlander — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                        |
| --- | ------------------------ | :-----: | ------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 20) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 20) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 20) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 20) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 20) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 20) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 20) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 20) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 20) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: 'return' outside function (<student>, line 20) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:

class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
		right = n

		while left < right:
			middle = left + (right - left) //2

		if isBadVersion(middle):
			right = middle
		else:
			left = middle + 1

	return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.

class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
		right = n

		while left < right:
			middle = left + (right - left) //2

		if isBadVersion(middle):
			right = middle
		else:
			left = middle + 1

	return left
```

</details>

---

### Nicholas Friedlander — TwoSum &nbsp;`7/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                     |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | got [], expected indices [1, 2]        |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                     |
| 4   | `all negatives t=-8`              | ❌ Fail | got [], expected indices [2, 4]        |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                     |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                     |
| 7   | `large target t=700`              | ✅ Pass | OK                                     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                     |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                     |
| 10  | `100-element array t=100`         | ❌ Fail | got [0, 98], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[inf], target: int) -> List[int]:
		left = 0
		right = len(nums) - 1

		while left < right:
			currSum = nums[left] + nums[right]

			if currSum == target:
				return [left, right]
			if currSum < target:
				left += 1
			else:
				right -= 1
		return []
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[inf], target: int) -> List[int]:
		left = 0
		right = len(nums) - 1

		while left < right:
			currSum = nums[left] + nums[right]

			if currSum == target:
				return [left, right]
			if currSum < target:
				left += 1
			else:
				right -= 1
		return []
```

</details>

---

### Noel Maldonado — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n
        while left < right:
            mid = left + (right - left) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n
        while left < right:
            mid = left + (right - left) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

---

### Noel Maldonado — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

---

### Parks Rpk — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n

        while left < right:
            mid = left + (right - left) // 2

            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1  # discard mid

        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n

        while left < right:
            mid = left + (right - left) // 2

            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1  # discard mid

        return left
```

</details>

---

### Parks Rpk — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                  |
| --- | --------------------------------- | :-----: | ------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | RUNTIME ERROR: NameError: name 'num_map' is not defined |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | RUNTIME ERROR: NameError: name 'num_map' is not defined |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | RUNTIME ERROR: NameError: name 'num_map' is not defined |
| 4   | `all negatives t=-8`              | ❌ Fail | RUNTIME ERROR: NameError: name 'num_map' is not defined |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | RUNTIME ERROR: NameError: name 'num_map' is not defined |
| 6   | `zeros t=0`                       | ❌ Fail | RUNTIME ERROR: NameError: name 'num_map' is not defined |
| 7   | `large target t=700`              | ❌ Fail | RUNTIME ERROR: NameError: name 'num_map' is not defined |
| 8   | `pair at end t=9`                 | ❌ Fail | RUNTIME ERROR: NameError: name 'num_map' is not defined |
| 9   | `two-element array t=10`          | ❌ Fail | RUNTIME ERROR: NameError: name 'num_map' is not defined |
| 10  | `100-element array t=100`         | ❌ Fail | RUNTIME ERROR: NameError: name 'num_map' is not defined |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
from typing import List

class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hash = {}

        for i in range(len(nums)):
            current = nums[i]
            complement = target - current

            if complement in num_map:
                return [hash[complement], i]

            hash[current] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
from typing import List

class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hash = {}

        for i in range(len(nums)):
            current = nums[i]
            complement = target - current

            if complement in num_map:
                return [hash[complement], i]

            hash[current] = i
```

</details>

---

### Reginald Juance — FBV &nbsp;`6/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                      |
| --- | ------------------------ | :-----: | ------------------------------------------- |
| 1   | `bad=first n=5`          | ✅ Pass | OK                                          |
| 2   | `bad=last n=5`           | ❌ Fail | got 5.5, expected 5                         |
| 3   | `bad=middle n=5`         | ✅ Pass | OK                                          |
| 4   | `single version n=1`     | ✅ Pass | OK                                          |
| 5   | `n=2 first bad`          | ✅ Pass | OK                                          |
| 6   | `n=2 second bad`         | ❌ Fail | got 2.5, expected 2                         |
| 7   | `large n bad=1`          | ✅ Pass | OK                                          |
| 8   | `large n bad=last`       | ❌ Fail | got 1000000000.1373549, expected 1000000000 |
| 9   | `large n bad=mid`        | ❌ Fail | got 500000000.63735485, expected 500000000  |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK                                          |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n):
        left = 1
        right = n

        while left < right:
            mid = (left + right) / 2

            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1

        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n):
        left = 1
        right = n

        while left < right:
            mid = (left + right) / 2

            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1

        return left
```

</details>

---

### Reginald Juance — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums, target):
        seen = {}

        for i in range(len(nums)):
            complement = target - nums[i]

            if complement in seen:
                return [seen[complement], i]

            seen[nums[i]] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums, target):
        seen = {}

        for i in range(len(nums)):
            complement = target - nums[i]

            if complement in seen:
                return [seen[complement], i]

            seen[nums[i]] = i
```

</details>

---

### Rijaa Zaidi — FBV &nbsp;`6/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                      |
| --- | ------------------------ | :-----: | ------------------------------------------- |
| 1   | `bad=first n=5`          | ✅ Pass | OK                                          |
| 2   | `bad=last n=5`           | ❌ Fail | got 5.5, expected 5                         |
| 3   | `bad=middle n=5`         | ✅ Pass | OK                                          |
| 4   | `single version n=1`     | ✅ Pass | OK                                          |
| 5   | `n=2 first bad`          | ✅ Pass | OK                                          |
| 6   | `n=2 second bad`         | ❌ Fail | got 2.5, expected 2                         |
| 7   | `large n bad=1`          | ✅ Pass | OK                                          |
| 8   | `large n bad=last`       | ❌ Fail | got 1000000000.1373549, expected 1000000000 |
| 9   | `large n bad=mid`        | ❌ Fail | got 500000000.63735485, expected 500000000  |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK                                          |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution(object):
    def firstBadVersion(self, n):
        l, r = 1, n

        while l < r:
            mid = (l+r)/2
            if isBadVersion(mid):
                r = mid
            else:
                l = mid + 1
        return l
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution(object):
    def firstBadVersion(self, n):
        l, r = 1, n

        while l < r:
            mid = (l+r)/2
            if isBadVersion(mid):
                r = mid
            else:
                l = mid + 1
        return l
```

</details>

---

### Rijaa Zaidi — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}
        for a in range(len(nums)):
            b = target - nums[a]
            if b in seen:
                return [seen[b], a]
            else:
                seen[nums[a]] = a
        return [0,0]
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}
        for a in range(len(nums)):
            b = target - nums[a]
            if b in seen:
                return [seen[b], a]
            else:
                seen[nums[a]] = a
        return [0,0]
```

</details>

---

### Ryan Martin — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
		low = 1
		high = n
		while (low < high):
			mid = (low + high)//2
			if isBadVersion(mid):
				high = mid
			else:
				low = mid + 1
		return low
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
		low = 1
		high = n
		while (low < high):
			mid = (low + high)//2
			if isBadVersion(mid):
				high = mid
			else:
				low = mid + 1
		return low
```

</details>

---

### Ryan Martin — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		checked = {}
		for i in range(len(nums)):
			diff = target - nums[i]
			if diff in checked:
				return [checked[diff], i]
			checked[nums[i]] = i
		return
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		checked = {}
		for i in range(len(nums)):
			diff = target - nums[i]
			if diff in checked:
				return [checked[diff], i]
			checked[nums[i]] = i
		return
```

</details>

---

### Ryan Tsui — FBV &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
#could use binary search for this question -> my solution in leetcode
#set a left in the front and right at the end of the list
        left = 1
        right = n
#perform binary search logic until the left is found
        while left < right:
            mid = (left + right) // 2

            if isBadVersion(mid):
                right = mid   # first bad is at mid or before
            else:
                left = mid + 1  # first bad is after mid

        return left
```

</details>

---

### Ryan Tsui — TwoSum &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
prevMap = {}
#basically every previous element that comes before current element stored -> val:index

        for i, n in enumerate(nums): #iterate through every value in this array
            diff = target - n #difference is target - n
            if diff in prevMap: #checking if difference is already in hashmap
                return[prevMap[diff], i]
#can return the solution which is the pair of the indices of the prevMap and i
            prevMap[n] = i #if dont find solution, update hashmap
        return #guarenteed solution
```

</details>

---

### Ryan Zhang — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution(object):
    def firstBadVersion(self, n):
        left = 1
        right = n
        while left < right:
            mid = (left + right) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution(object):
    def firstBadVersion(self, n):
        left = 1
        right = n
        while left < right:
            mid = (left + right) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

---

### Ryan Zhang — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		num_dict = {}
		for i, num in enumerate(nums):
			complement = target - num
			if complement in num_dict:
				return [num_dict[complement], i]
			num_dict[num] = i
		return None
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		num_dict = {}
		for i, num in enumerate(nums):
			complement = target - num
			if complement in num_dict:
				return [num_dict[complement], i]
			num_dict[num] = i
		return None
```

</details>

---

### Samuel Halsband — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
# The isBadVersion API is already defined for you.
# def isBadVersion(version: int) -> bool:

class Solution:
    def firstBadVersion(self, n: int) -> int:
        low = 0
        high = n
        while low < high:
            mid = (low + high) // 2
            if isBadVersion(mid):
                if not isBadVersion(mid-1): # unnecessary but speeds up program so it doesn't have to go through entire binary search in case we're already at the first bad version
                    return mid
                high = mid
            else:
                low = mid + 1
        return high
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

# The isBadVersion API is already defined for you.

class Solution:
    def firstBadVersion(self, n: int) -> int:
        low = 0
        high = n
        while low < high:
            mid = (low + high) // 2
            if isBadVersion(mid):
                if not isBadVersion(mid-1): # unnecessary but speeds up program so it doesn't have to go through entire binary search in case we're already at the first bad version
                    return mid
                high = mid
            else:
                low = mid + 1
        return high
```

</details>

---

### Samuel Halsband — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        table = {}
        for i, num in enumerate(nums):
            if (target-num) in table:
                return [table[target-num],i]
            table[num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        table = {}
        for i, num in enumerate(nums):
            if (target-num) in table:
                return [table[target-num],i]
            table[num] = i
```

</details>

---

### Santiago Zuluaga — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
                right = n

        while left < right;
                 mid = (left + right) // 2

                 if isBadVersion(mid):
                     right = mid
                 else:
                     left = mid + 1
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
		left = 1
                right = n

        while left < right;
                 mid = (left + right) // 2

                 if isBadVersion(mid):
                     right = mid
                 else:
                     left = mid + 1
        return left
```

</details>

---

### Santiago Zuluaga — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:

        map = {}

        for i, n in enumerate(nums):
            diff = target - n
            if diff in map:
               return [map[diff], i]
            map[n] = i
        return
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:

        map = {}

        for i, n in enumerate(nums):
            diff = target - n
            if diff in map:
               return [map[diff], i]
            map[n] = i
        return
```

</details>

---

### Shunyi Chen — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n):
        left = 1
        right = n
        while left < right:
            mid = left + (right - left) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n):
        left = 1
        right = n
        while left < right:
            mid = left + (right - left) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

---

### Shunyi Chen — FBV &nbsp;`—`

**Status:** `NO SUBMISSION`

> ⚠️ Student did not submit an answer.

---

### Shunyi Chen — TwoSum &nbsp;`9/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK                                     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                     |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                     |
| 7   | `large target t=700`              | ✅ Pass | OK                                     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                     |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                     |
| 10  | `100-element array t=100`         | ❌ Fail | got [0, 98], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            for j in range(i + 1, len(nums)):
                if nums[i] + nums[j] == target:
                    return [i, j]
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            for j in range(i + 1, len(nums)):
                if nums[i] + nums[j] == target:
                    return [i, j]
```

</details>

---

### Shunyi Chen — TwoSum &nbsp;`—`

**Status:** `NO SUBMISSION`

> ⚠️ Student did not submit an answer.

---

### Solomon Coverdale — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        low = 1
        high = n
        while low < high:
            mid = (low + high)//2
            if isBadVersion(mid):
                high = mid
            else:
                low = mid +1
        return low
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        low = 1
        high = n
        while low < high:
            mid = (low + high)//2
            if isBadVersion(mid):
                high = mid
            else:
                low = mid +1
        return low
```

</details>

---

### Solomon Coverdale — TwoSum &nbsp;`9/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK                                     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK                                     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK                                     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK                                     |
| 6   | `zeros t=0`                       | ✅ Pass | OK                                     |
| 7   | `large target t=700`              | ✅ Pass | OK                                     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK                                     |
| 9   | `two-element array t=10`          | ✅ Pass | OK                                     |
| 10  | `100-element array t=100`         | ❌ Fail | got [0, 98], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        dict = {}
        for i in range(len(nums)):
            dict[nums[i]] = i

        for i in range(len(nums)):
            search = target - nums[i]
            if search in dict and dict[search] != i:
                return i,dict[search]
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        dict = {}
        for i in range(len(nums)):
            dict[nums[i]] = i

        for i in range(len(nums)):
            search = target - nums[i]
            if search in dict and dict[search] != i:
                return i,dict[search]
```

</details>

---

### Stephania Calin — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left, right = 1, n

		while left < right:
			mid = (left + right) // 2
			if isBadVersion(mid):
				right = mid
			else:
				left = mid + 1

		return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
		left, right = 1, n

		while left < right:
			mid = (left + right) // 2
			if isBadVersion(mid):
				right = mid
			else:
				left = mid + 1

		return left
```

</details>

---

### Stephania Calin — TwoSum &nbsp;`1/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                 |
| --- | --------------------------------- | :-----: | -------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | got [1, 1], expected indices [0, 1]    |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK                                     |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | got [1, 1], expected indices [0, 1]    |
| 4   | `all negatives t=-8`              | ❌ Fail | got [1, 4], expected indices [2, 4]    |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | got [1, 2], expected indices [0, 2]    |
| 6   | `zeros t=0`                       | ❌ Fail | got [1, 3], expected indices [0, 3]    |
| 7   | `large target t=700`              | ❌ Fail | got [1, 3], expected indices [2, 3]    |
| 8   | `pair at end t=9`                 | ❌ Fail | got [1, 4], expected indices [3, 4]    |
| 9   | `two-element array t=10`          | ❌ Fail | got [1, 1], expected indices [0, 1]    |
| 10  | `100-element array t=100`         | ❌ Fail | got [1, 50], expected indices [48, 50] |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#assuming theres an import? but if not supposed to assume, the line should be:
from typing import List

class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		seen = {}

		for i , num in enumerate(nums):
			complement = target - num
			if complement in seen:
				return [seen[complement], i]
			seen[num] = 1
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
#assuming theres an import? but if not supposed to assume, the line should be:
from typing import List

class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		seen = {}

		for i , num in enumerate(nums):
			complement = target - num
			if complement in seen:
				return [seen[complement], i]
			seen[num] = 1
```

</details>

---

### Sude Genc — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Wrapped bare `def firstBadVersion(...)` in `class Solution:`.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
def firstBadVersion(self, n: int) -> int:
    left, right = 1, n
    while left < right:
        mid = left + (right - left) // 2
        if isBadVersion(mid):
            right = mid
        else:
            left = mid + 1
    return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left, right = 1, n
        while left < right:
            mid = left + (right - left) // 2
            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

---

### Sude Genc — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                                                            |
| --- | --------------------------------- | :-----: | ------------------------------------------------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 3 (<student>, line 4) |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 3 (<student>, line 4) |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 3 (<student>, line 4) |
| 4   | `all negatives t=-8`              | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 3 (<student>, line 4) |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 3 (<student>, line 4) |
| 6   | `zeros t=0`                       | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 3 (<student>, line 4) |
| 7   | `large target t=700`              | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 3 (<student>, line 4) |
| 8   | `pair at end t=9`                 | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 3 (<student>, line 4) |
| 9   | `two-element array t=10`          | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 3 (<student>, line 4) |
| 10  | `100-element array t=100`         | ❌ Fail | COMPILE ERROR: expected an indented block after function definition on line 3 (<student>, line 4) |

**Edits made by the grader:**

1. Wrapped bare `def twoSum(...)` in `class Solution:` so it matches the expected `Solution().twoSum(...)` call interface.
2. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
def twoSum(nums, target):
for i in range(len(nums)-1):
         for j in range (i+1,len(nums)):
                 if(nums[i]+nums[j]==target):
                                return i, j
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(nums, target):
    for i in range(len(nums)-1):
             for j in range (i+1,len(nums)):
                     if(nums[i]+nums[j]==target):
                                    return i, j
```

</details>

---

### Tianna Balkam — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n
        while left < right:
            mid = (left + right) // 2
            if isBadVersion(mid):
                right = mid # first bad version could be mid or earlier
            else:
                left = mid + 1 # first bad version is after mid
        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n
        while left < right:
            mid = (left + right) // 2
            if isBadVersion(mid):
                right = mid # first bad version could be mid or earlier
            else:
                left = mid + 1 # first bad version is after mid
        return left
```

</details>

---

### Tiffany Lin — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 9) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
		l,r = 1,n
                while l < r:
                    m = l+(r-l)//2
                    if isBadVersion(m):
                        r = m
                    else:
                        l =m +1
                 return l
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
		l,r = 1,n
                while l < r:
                    m = l+(r-l)//2
                    if isBadVersion(m):
                        r = m
                    else:
                        l =m +1
                 return l
```

</details>

---

### Treyson Thelusma — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
            l = 0
            r = n
            while l <= r:
                m = (l + r) // 2
                if isBadVersion(m):
                    r = m - 1
                else:
                    l = m + 1
            return l
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
            l = 0
            r = n
            while l <= r:
                m = (l + r) // 2
                if isBadVersion(m):
                    r = m - 1
                else:
                    l = m + 1
            return l
```

</details>

---

### Treyson Thelusma — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
            seen = {}
            for i in range(len(nums)):
                complement = target - nums[i]
                if (complement) in seen:
                    return [i, seen[complement]]
                seen[nums[i]] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
            seen = {}
            for i in range(len(nums)):
                complement = target - nums[i]
                if (complement) in seen:
                    return [i, seen[complement]]
                seen[nums[i]] = i
```

</details>

---

### Varun Karamchandani — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        lo, hi = 1, n
        while lo < hi:
            mid = (lo + hi) // 2
            if isBadVersion(mid):
                hi = mid
            else:
                lo = mid + 1
        return lo
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        lo, hi = 1, n
        while lo < hi:
            mid = (lo + hi) // 2
            if isBadVersion(mid):
                hi = mid
            else:
                lo = mid + 1
        return lo
```

</details>

---

### Varun Karamchandani — TwoSum &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail |
| --- | --------------------------------- | :-----: | ------ |
| 1   | `basic [2,7,11,15] t=9`           | ✅ Pass | OK     |
| 2   | `answer not at start [3,2,4] t=6` | ✅ Pass | OK     |
| 3   | `duplicates [3,3] t=6`            | ✅ Pass | OK     |
| 4   | `all negatives t=-8`              | ✅ Pass | OK     |
| 5   | `mixed pos/neg t=0`               | ✅ Pass | OK     |
| 6   | `zeros t=0`                       | ✅ Pass | OK     |
| 7   | `large target t=700`              | ✅ Pass | OK     |
| 8   | `pair at end t=9`                 | ✅ Pass | OK     |
| 9   | `two-element array t=10`          | ✅ Pass | OK     |
| 10  | `100-element array t=100`         | ✅ Pass | OK     |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}  # value -> index
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        seen = {}  # value -> index
        for i, num in enumerate(nums):
            complement = target - num
            if complement in seen:
                return [seen[complement], i]
            seen[num] = i
```

</details>

---

### Ved Patel — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                               |
| --- | --------------------------------- | :-----: | ---------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | COMPILE ERROR: unexpected indent (<student>, line 9) |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | COMPILE ERROR: unexpected indent (<student>, line 9) |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | COMPILE ERROR: unexpected indent (<student>, line 9) |
| 4   | `all negatives t=-8`              | ❌ Fail | COMPILE ERROR: unexpected indent (<student>, line 9) |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | COMPILE ERROR: unexpected indent (<student>, line 9) |
| 6   | `zeros t=0`                       | ❌ Fail | COMPILE ERROR: unexpected indent (<student>, line 9) |
| 7   | `large target t=700`              | ❌ Fail | COMPILE ERROR: unexpected indent (<student>, line 9) |
| 8   | `pair at end t=9`                 | ❌ Fail | COMPILE ERROR: unexpected indent (<student>, line 9) |
| 9   | `two-element array t=10`          | ❌ Fail | COMPILE ERROR: unexpected indent (<student>, line 9) |
| 10  | `100-element array t=100`         | ❌ Fail | COMPILE ERROR: unexpected indent (<student>, line 9) |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		#Write your answer here

               hash_map = {}
               complement = 0

                for i, elem in enumerate(nums):
                         complement = target - elem

                          if complement in hash_map:
                                 return [hash_map[complement], i]

                          hash_map[elem] = i

                return [-1, -1]
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		#Write your answer here

               hash_map = {}
               complement = 0

                for i, elem in enumerate(nums):
                         complement = target - elem

                          if complement in hash_map:
                                 return [hash_map[complement], i]

                          hash_map[elem] = i

                return [-1, -1]
```

</details>

---

### Vikram Minhas — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def firstBadVersion(self, n: int) -> int:
		l = 1
                r = n
                while l < r:
                         mid = (l + r) / 2
                         if (isBadVersion(mid)):
                             r = mid:
                         else:
                             l = mid + 1
                return l
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
	def firstBadVersion(self, n: int) -> int:
		l = 1
                r = n
                while l < r:
                         mid = (l + r) / 2
                         if (isBadVersion(mid)):
                             r = mid:
                         else:
                             l = mid + 1
                return l
```

</details>

---

### Vikram Minhas — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                                                |
| --- | --------------------------------- | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 4   | `all negatives t=-8`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 6   | `zeros t=0`                       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 7   | `large target t=700`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 8   | `pair at end t=9`                 | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 9   | `two-element array t=10`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |
| 10  | `100-element array t=100`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 5) |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		hash_table = {}
                for i in range(len(nums)):
                      pair = target - nums[i]
                      if pair in hash_table:
                             return (hash_table[pair], i)
                      hash_table[nums[i]] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
		hash_table = {}
                for i in range(len(nums)):
                      pair = target - nums[i]
                      if pair in hash_table:
                             return (hash_table[pair], i)
                      hash_table[nums[i]] = i
```

</details>

---

### William Connors — FBV &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                                                                                |
| --- | ------------------------ | :-----: | ------------------------------------------------------------------------------------- |
| 1   | `bad=first n=5`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 2   | `bad=last n=5`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 3   | `bad=middle n=5`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 4   | `single version n=1`     | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 5   | `n=2 first bad`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 6   | `n=2 second bad`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 7   | `large n bad=1`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 8   | `large n bad=last`       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 9   | `large n bad=mid`        | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |
| 10  | `bad=second-to-last n=5` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 8) |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
#The isBadVersion API is already defined for you.
#def isBadVersion(version: int) -> bool:
class Solution:
	def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

while left < right:
          mid = left + (right - left) //2

          if isBadVersion(mid):
              right = mid

          else:
                left = mid + 1

return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

#The isBadVersion API is already defined for you.
class Solution:
	def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

while left < right:
          mid = left + (right - left) //2

          if isBadVersion(mid):
              right = mid

          else:
                left = mid + 1

return left
```

</details>

---

### William Connors — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                                                  |
| --- | --------------------------------- | :-----: | --------------------------------------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 12) |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 12) |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 12) |
| 4   | `all negatives t=-8`              | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 12) |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 12) |
| 6   | `zeros t=0`                       | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 12) |
| 7   | `large target t=700`              | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 12) |
| 8   | `pair at end t=9`                 | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 12) |
| 9   | `two-element array t=10`          | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 12) |
| 10  | `100-element array t=100`         | ❌ Fail | COMPILE ERROR: unindent does not match any outer indentation level (<student>, line 12) |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
         value_to_index = {}

for i, num in enumerate(nums):
      complement = target - num

      if complement in value_to_index:
          return [value_to_index[compliment], i]

       value_to_index [num] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
	def twoSum(self, nums: List[int], target: int) -> List[int]:
         value_to_index = {}

for i, num in enumerate(nums):
      complement = target - num

      if complement in value_to_index:
          return [value_to_index[compliment], i]

       value_to_index [num] = i
```

</details>

---

### William Conroy — FBV &nbsp;`10/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail |
| --- | ------------------------ | :-----: | ------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK     |
| 2   | `bad=last n=5`           | ✅ Pass | OK     |
| 3   | `bad=middle n=5`         | ✅ Pass | OK     |
| 4   | `single version n=1`     | ✅ Pass | OK     |
| 5   | `n=2 first bad`          | ✅ Pass | OK     |
| 6   | `n=2 second bad`         | ✅ Pass | OK     |
| 7   | `large n bad=1`          | ✅ Pass | OK     |
| 8   | `large n bad=last`       | ✅ Pass | OK     |
| 9   | `large n bad=mid`        | ✅ Pass | OK     |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK     |

**Edits made by the grader:**

1. Removed template `#def isBadVersion` comment to avoid shadowing the injected test mock.
2. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
# The isBadVersion API is already defined for you.
# def isBadVersion(version: int) -> bool:

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

        while left < right:
            mid = left + (right - left) // 2

            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1

        return left
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

# The isBadVersion API is already defined for you.

class Solution:
    def firstBadVersion(self, n: int) -> int:
        left = 1
        right = n

        while left < right:
            mid = left + (right - left) // 2

            if isBadVersion(mid):
                right = mid
            else:
                left = mid + 1

        return left
```

</details>

---

### William Conroy — TwoSum &nbsp;`0/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                         | Result  | Detail                                                                                 |
| --- | --------------------------------- | :-----: | -------------------------------------------------------------------------------------- |
| 1   | `basic [2,7,11,15] t=9`           | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 2   | `answer not at start [3,2,4] t=6` | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 3   | `duplicates [3,3] t=6`            | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 4   | `all negatives t=-8`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 5   | `mixed pos/neg t=0`               | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 6   | `zeros t=0`                       | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 7   | `large target t=700`              | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 8   | `pair at end t=9`                 | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 9   | `two-element array t=10`          | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |
| 10  | `100-element array t=100`         | ❌ Fail | COMPILE ERROR: inconsistent use of tabs and spaces in indentation (<student>, line 10) |

**Edits made by the grader:**

1. Prepended `from typing import List` to satisfy `List[int]` type hints.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        pairMap= {}
        for i in range(len(nums)):
            n = nums[i]
            match = target - n

            if match in pairMap:
	        print("[", pairMap[match], ",", i, "]")
                return [pairMap[match], i]

            pairMap[n] = i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        pairMap= {}
        for i in range(len(nums)):
            n = nums[i]
            match = target - n

            if match in pairMap:
	        print("[", pairMap[match], ",", i, "]")
                return [pairMap[match], i]

            pairMap[n] = i
```

</details>

---

### Xinlin Wu — FBV &nbsp;`8/10`

**Status:** `TESTED`

**Test results:**

| #   | Test Case                | Result  | Detail                               |
| --- | ------------------------ | :-----: | ------------------------------------ |
| 1   | `bad=first n=5`          | ✅ Pass | OK                                   |
| 2   | `bad=last n=5`           | ✅ Pass | OK                                   |
| 3   | `bad=middle n=5`         | ✅ Pass | OK                                   |
| 4   | `single version n=1`     | ✅ Pass | OK                                   |
| 5   | `n=2 first bad`          | ✅ Pass | OK                                   |
| 6   | `n=2 second bad`         | ✅ Pass | OK                                   |
| 7   | `large n bad=1`          | ✅ Pass | OK                                   |
| 8   | `large n bad=last`       | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |
| 9   | `large n bad=mid`        | ❌ Fail | TIMEOUT (>3s) — likely infinite loop |
| 10  | `bad=second-to-last n=5` | ✅ Pass | OK                                   |

**Edits made by the grader:**

1. Prepended `from typing import List` and injected a `isBadVersion(v)` mock that returns `True` when `v >= bad`. The `bad` value is swapped per test case at runtime.

<details>
<summary><strong>Original code</strong> (exactly as submitted)</summary>

```python
class Solution:
    def firstBadVersion(self, n: int) -> int:
        for i in range(1, n+1):
            if isBadVersion(i):
                return i
```

</details>

<details>
<summary><strong>Modified code</strong> (what the grader executed)</summary>

```python
from typing import List

def isBadVersion(v): return v >= 4  # injected test mock

class Solution:
    def firstBadVersion(self, n: int) -> int:
        for i in range(1, n+1):
            if isBadVersion(i):
                return i
```

</details>

---

### Zhi Xiong Lu — FBV &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
Initialize left = 1
initialize right = n

when left is less than right

middle = (left+right)/2

if middle is a bad version
right = middle

else
left = middle + 1
```

</details>

---

### Zhi Xiong Lu — TwoSum &nbsp;`—`

**Status:** `PSEUDOCODE`

> ⚠️ Answer appears to be pseudocode or plain English — no executable Python detected.

<details>
<summary>Original submission</summary>

```python
Initialize empty hashmap

Loop through the array

Let Complement = Target - Current

If complement is already in hashmap
Return position of complement and current index

Else
Store current number and index in hashmap
```

</details>

---
