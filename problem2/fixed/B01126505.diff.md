# Syntax repair diff — B01126505

## Changes

- **[syntax-only]** Java → Python mechanical transpilation (same logic preserved)

## Unified diff
```diff
--- raw
+++ fixed
@@ -1,14 +1,11 @@
-public static int countPairs(int[] nums, int T) {

-    int l = 0;

-    int r = nums.length-1;

-    int count = 0;

+def countPairs(nums, T):

+    l = 0

+    r = len(nums)-1

+    count = 0

 

-    while(l>r) {

-        if(nums[l]+nums[r]<=T) {

-            count += (r-l);

-            l++;

-        }

-        else

-            r--;

-    }

-}
+    while l>r:

+        if nums[l]+nums[r]<=T:

+            count += (r-l)

+            l += 1

+        else:

+            r -= 1
```