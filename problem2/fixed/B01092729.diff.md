# Syntax repair diff — B01092729

## Changes

- **[syntax-only]** Re-indented 8 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -8,11 +8,11 @@
         if (nums[leftptr] + nums[rightptr] <= T):

             if (nums[leftptr] < nums[rightptr]):

                 validPairs+=1;

-            rightptr+=1;

-            if(rightptr >= len(nums)):

-                leftptr += 1;

-                rightptr = leftptr+1;

-        else:

-            leftptr += 1;

-            rightptr = leftptr+1;

-    return validPairs;
+                rightptr+=1;

+                if(rightptr >= len(nums)):

+                    leftptr += 1;

+                    rightptr = leftptr+1;

+                else:

+                    leftptr += 1;

+                    rightptr = leftptr+1;

+                    return validPairs;
```