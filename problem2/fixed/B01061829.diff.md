# Syntax repair diff — B01061829

## Changes

- **[syntax-only]** Re-indented 7 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -1,15 +1,15 @@
 def count_pairs(nums, T):

-     left = 0

-     right = len(nums) - 1

-     count = 0

+    left = 0

+    right = len(nums) - 1

+    count = 0

 

-     while left < right:

-          current_sum = nums[left] + nums[right]

+    while left < right:

+        current_sum = nums[left] + nums[right]

 

-           if current_sum <= T:

+        if current_sum <= T:

             count += right - left

             left += 1

         else:

             right -= 1

 

-    return count
+            return count
```