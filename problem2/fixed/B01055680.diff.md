# Syntax repair diff — B01055680

## Changes

- **[syntax-only]** Java → Python mechanical transpilation (same logic preserved)

## Unified diff
```diff
--- raw
+++ fixed
@@ -1,16 +1,13 @@
-public static int countPairs(int[] nums, int T) {

-    int left = 0;

-    int right = nums.length - 1;

-    int count = 0;

+def countPairs(nums, T):

+    left = 0

+    right = len(nums) - 1

+    count = 0

 

-    while (left < right) {

-        if (nums[left] + nums[right] <= T) {

-            count += (right - left);

-            left++;  

-        } else {

-            right--;

-        }

-    }

-    return count;

-}

+    while left < right:

+        if nums[left] + nums[right] <= T:

+            count += (right - left)

+            left += 1

+            } else {

+            right -= 1

+            return count

 

```