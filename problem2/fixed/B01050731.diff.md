# Syntax repair diff — B01050731

## Changes

- **[syntax-only]** Re-indented 10 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -1,11 +1,11 @@
 def count_pairs(nums, T):

-	count = 0

-        left = 0

-        right = len(nums) - 1

-        while left < right:

-            if nums[left] + nums[right] <= T:

-                  count += right - left

-                  left += 1

-            else:

-                  right -= 1

-         return count
+    count = 0

+    left = 0

+    right = len(nums) - 1

+    while left < right:

+        if nums[left] + nums[right] <= T:

+            count += right - left

+            left += 1

+        else:

+            right -= 1

+            return count
```