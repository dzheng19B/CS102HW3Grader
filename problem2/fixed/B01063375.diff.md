# Syntax repair diff — B01063375

## Changes

- **[syntax-only]** Re-indented 6 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -7,11 +7,11 @@
 

     While left < right

 

-        If nums[left] + nums[right] is <= T

-            Add (right - left) to counter

-            Increment left by 1

+    If nums[left] + nums[right] is <= T

+    Add (right - left) to counter

+    Increment left by 1

 

-        Else:

-            Increment right by -1

- 

-    return count

+    Else:

+        Increment right by -1

+

+        return count

```