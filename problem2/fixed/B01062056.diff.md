# Syntax repair diff — B01062056

## Changes

- **[syntax-only]** Re-indented 10 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -1,13 +1,13 @@
 def count_pairs(nums, T):

-    	left = 0

-    	right = len(nums) - 1

-    	count = 0

+    left = 0

+    right = len(nums) - 1

+    count = 0

 

-    	while left < right:

-       		if nums[left] + nums[right] <= T:

-  			count += (right - left)

-            		left += 1

-        	else:

-            		right -= 1

+    while left < right:

+        if nums[left] + nums[right] <= T:

+            count += (right - left)

+            left += 1

+        else:

+            right -= 1

 

-    	return count
+            return count
```