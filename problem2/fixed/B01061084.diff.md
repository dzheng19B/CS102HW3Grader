# Syntax repair diff — B01061084

## Changes

- **[syntax-only]** Line 8: added missing colon to `else`

## Unified diff
```diff
--- raw
+++ fixed
@@ -5,7 +5,7 @@
  

 while j >= i:

  j -= 1

-else

+else:

   pairs = nums[i] + nums[j]

   while pairs <= T:

     count += (j - i)

```