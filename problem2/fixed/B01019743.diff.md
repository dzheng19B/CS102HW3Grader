# Syntax repair diff — B01019743

## Changes

- **[syntax-only]** Line 8: replaced C-style `//` comment with `#`
- **[syntax-only]** Line 9: replaced C-style `//` comment with `#`
- **[syntax-only]** Line 11: replaced C-style `//` comment with `#`
- **[syntax-only]** Re-indented 11 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -1,12 +1,12 @@
 def count_pairs(nums, T):

-	# Paste this into your answer if you want to use Python

-        left = 0

-        right = len(nums) - 1

-        total_pairs = 0

-        while left < right:

-              if nums[left] + nums[right] <= T:

-                      total_pairs = right - left // updates with pointers

-                      left += 1 // shift left

-              else:

-                      right+=1 // shift right if not

-          return total_pairs
+    # Paste this into your answer if you want to use Python

+    left = 0

+    right = len(nums) - 1

+    total_pairs = 0

+    while left < right:

+        if nums[left] + nums[right] <= T:

+            total_pairs = right - left  # updates with pointers

+            left += 1  # shift left

+        else:

+            right+=1  # shift right if not

+            return total_pairs
```