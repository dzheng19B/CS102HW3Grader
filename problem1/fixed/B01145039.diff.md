# Syntax repair diff — B01145039

## Changes

- **[syntax-only]** Re-indented 10 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -7,13 +7,13 @@
         sum += nums[r]

         if nums[r] > 0:

             neg++

-        if r - l >= k:

-            if nums[l] < 0:

-                neg--

-            sum -= nums[l]

-            left++

-        if r - l == k - 1:

-            if neg < 2:

-                if sum > maxsum:

-                      maxsum = sum

-    return maxsum
+            if r - l >= k:

+                if nums[l] < 0:

+                    neg--

+                    sum -= nums[l]

+                    left++

+                    if r - l == k - 1:

+                        if neg < 2:

+                            if sum > maxsum:

+                                maxsum = sum

+                                return maxsum
```