# Syntax repair diff — B01121023

## Changes

- **[syntax-only]** Re-indented 26 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -14,36 +14,36 @@
             print(num)

 

 

-Question 8.)

+            Question 8.)

 

-def max_valid_window_sum(nums, k):

-    window_sum = 0

-    negative_count = 0

-    max_sum = 0

-    found_valid = False

+            def max_valid_window_sum(nums, k):

+                window_sum = 0

+                negative_count = 0

+                max_sum = 0

+                found_valid = False

 

-    for i in range(k):

-        window_sum += nums[i]

-        if nums[i] < 0:

-            negative_count += 1

+                for i in range(k):

+                    window_sum += nums[i]

+                    if nums[i] < 0:

+                        negative_count += 1

 

-    if negative_count <= 1:

-        max_sum = window_sum

-        found_valid = True

+                        if negative_count <= 1:

+                            max_sum = window_sum

+                            found_valid = True

 

-    for i in range(k, len(nums)):

-        # Remove left element

-        if nums[i - k] < 0:

-            negative_count -= 1

-        window_sum -= nums[i - k]

+                            for i in range(k, len(nums)):

+                                # Remove left element

+                                if nums[i - k] < 0:

+                                    negative_count -= 1

+                                    window_sum -= nums[i - k]

 

-        if nums[i] < 0:

-            negative_count += 1

-        window_sum += nums[i]

+                                    if nums[i] < 0:

+                                        negative_count += 1

+                                        window_sum += nums[i]

 

-        if negative_count <= 1:

-            if not found_valid or window_sum > max_sum:

-                max_sum = window_sum

-                found_valid = True

+                                        if negative_count <= 1:

+                                            if not found_valid or window_sum > max_sum:

+                                                max_sum = window_sum

+                                                found_valid = True

 

-    return max_sum if found_valid else 0

+                                                return max_sum if found_valid else 0

```