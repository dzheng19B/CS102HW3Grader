# Syntax repair diff — B01137729

## Changes

- **[syntax-only]** Re-indented 13 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -5,18 +5,18 @@
     if nums[i] < 0:

         negCount++

 

-total= 0

+        total= 0

 

-if negCount <= 1:

-    total = sum 

-for i from k to len(nums) - 1:

-    sum += nums[i]       

-    sum -= nums[i - k]     

-    if nums[i] <0: 

-        negCount++  

-    if nums[i - k] < 0: 

-        negCount-- 

-    if negCount <= 1:

-        total = maxOf(total, sum)

+        if negCount <= 1:

+            total = sum

+            for i from k to len(nums) - 1:

+                sum += nums[i]

+                sum -= nums[i - k]

+                if nums[i] <0:

+                    negCount++

+                    if nums[i - k] < 0:

+                        negCount--

+                        if negCount <= 1:

+                            total = maxOf(total, sum)

 

-return total
+                            return total
```