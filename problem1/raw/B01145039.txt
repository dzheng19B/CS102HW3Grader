def max_valid_window_sum(nums, k):
    neg = 0
    sum = 0
    maxsum = 0
    l = 0
    for r in range(len(nums)):
        sum += nums[r]
        if nums[r] > 0:
            neg++
        if r - l >= k:
            if nums[l] < 0:
                neg--
            sum -= nums[l]
            left++
        if r - l == k - 1:
            if neg < 2:
                if sum > maxsum:
                      maxsum = sum
    return maxsum