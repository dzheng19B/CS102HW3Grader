# Syntax repair diff — B01129735

## Changes

- **[syntax-only]** Removed 1 trailing natural-language line(s) (prose explanation mixed into answer)

## Unified diff
```diff
--- raw
+++ fixed
@@ -16,6 +16,4 @@
         if min_dist != float('inf'):

             return min_dist

         else:

-            return -1

-

-The time complexity would be O(n) since there is only one for loop to go through the list one time, then they get put into a dictionary. This solves the Evalute in UMPIRE because it is basically explaining the thought process behind it.
+            return -1
```