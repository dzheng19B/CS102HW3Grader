# Syntax repair diff — B01132918

## Changes

- **[syntax-only]** Line 2: replaced C-style `//` comment with `#`
- **[syntax-only]** Line 3: replaced C-style `//` comment with `#`
- **[syntax-only]** Line 8: added missing colon to `for (let i = 0; i < nums.length; i++) {`
- **[syntax-only]** Line 11: added missing colon to `if (!lastSeen.has(num)) {`
- **[syntax-only]** Line 17: added missing colon to `if (seen.length !== 2) {`
- **[syntax-only]** Re-indented 19 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -1,29 +1,29 @@
 ```ts

-// 3740. Minimum Distance Between Three Equal Elements I

-// TypeScript solution

+# 3740. Minimum Distance Between Three Equal Elements I

+# TypeScript solution

 function minimumDistance(nums: number[]): number {

-    const lastSeen = new Map<number, number[]>();

-    let minDistance = Infinity;

+const lastSeen = new Map<number, number[]>();

+let minDistance = Infinity;

 

-    for (let i = 0; i < nums.length; i++) {

-        const num = nums[i];

+for (let i = 0; i < nums.length; i++) {:

+    const num = nums[i];

 

-        if (!lastSeen.has(num)) {

-            lastSeen.set(num, [i]);

-            continue;

-        }

+    if (!lastSeen.has(num)) {:

+        lastSeen.set(num, [i]);

+        continue;

+    }

 

-        const seen = lastSeen.get(num)! as [number, number];

-        if (seen.length !== 2) {

-            lastSeen.set(num, [seen[0], i]);

-            continue;

-        }

+    const seen = lastSeen.get(num)! as [number, number];

+    if (seen.length !== 2) {:

+        lastSeen.set(num, [seen[0], i]);

+        continue;

+    }

 

-        const [j, k] = seen;

-        const distance = Math.abs(i - j) + Math.abs(j - k) + Math.abs(k - i);

-        minDistance = Math.min(distance, minDistance);

+    const [j, k] = seen;

+    const distance = Math.abs(i - j) + Math.abs(j - k) + Math.abs(k - i);

+    minDistance = Math.min(distance, minDistance);

 

-        lastSeen.set(num, [k, i]);

+    lastSeen.set(num, [k, i]);

     }

 

     return minDistance === Infinity ? -1 : minDistance;

```