# Syntax repair diff — B01171535

## Changes

- **[syntax-only]** Line 1: added missing colon to `class Solution {`
- **[syntax-only]** Line 6: added missing colon to `for (int i = 0; i < nums.size(); i++) {`
- **[syntax-only]** Line 10: added missing colon to `for (auto& [val, indices] : indexMap) {`
- **[syntax-only]** Line 11: added missing colon to `if (indices.size() < 3) continue;`
- **[syntax-only]** Line 12: added missing colon to `for (int t = 0; t + 2 < indices.size(); t++) {`
- **[syntax-only]** Re-indented 21 line(s) whose indentation was broken by textbox paste

## Unified diff
```diff
--- raw
+++ fixed
@@ -1,26 +1,26 @@
-class Solution {

-public:

+class Solution {:

+    public:

 

-int minimumDistance(vector<int>& nums) {

-    unordered_map<int, vector<int>> indexMap;

-    for (int i = 0; i < nums.size(); i++) {

-        indexMap[nums[i]].push_back(i);

-    }

-    int minDist = INT_MAX;

-    for (auto& [val, indices] : indexMap) {

-        if (indices.size() < 3) continue;

-        for (int t = 0; t + 2 < indices.size(); t++) {

-            int i = indices[t];

-            int j = indices[t + 1];

-            int k = indices[t + 2];

-            int dist = 2 * (k - i);

-            minDist = min(minDist, dist);

-        }

-    }

-    return minDist == INT_MAX ? -1 : minDist;

-}

-};

+        int minimumDistance(vector<int>& nums) {

+        unordered_map<int, vector<int>> indexMap;

+        for (int i = 0; i < nums.size(); i++) {:

+            indexMap[nums[i]].push_back(i);

+            }

+            int minDist = INT_MAX;

+            for (auto& [val, indices] : indexMap) {:

+                if (indices.size() < 3) continue;:

+                    for (int t = 0; t + 2 < indices.size(); t++) {:

+                        int i = indices[t];

+                        int j = indices[t + 1];

+                        int k = indices[t + 2];

+                        int dist = 2 * (k - i);

+                        minDist = min(minDist, dist);

+                        }

+                        }

+                        return minDist == INT_MAX ? -1 : minDist;

+                    }

+                    };

 

 

 

-I group indices by value using a hash map, then for each value with at least 3 occurrences, I slide over consecutive triples and compute 2 * (k - i) as the simplified distance. Since indices are stored in sorted order, consecutive triples always minimize k - i, guaranteeing the optimal result. Overall time complexity is O(n) since every index is visited at most once.
+                    I group indices by value using a hash map, then for each value with at least 3 occurrences, I slide over consecutive triples and compute 2 * (k - i) as the simplified distance. Since indices are stored in sorted order, consecutive triples always minimize k - i, guaranteeing the optimal result. Overall time complexity is O(n) since every index is visited at most once.
```